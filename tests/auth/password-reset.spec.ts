import { test, expect } from '@playwright/test'

test.describe('Password reset flow (AUTH-06)', () => {
  test.skip('reset-password page renders email input and submit button', async () => {})
  test.skip('submitting a valid email shows confirmation message', async () => {})
  test.skip('reset-password/update page renders new password and confirm password fields', async () => {})
  // Note: full email delivery cannot be tested in CI — manual verification required
})
