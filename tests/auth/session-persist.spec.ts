import { test, expect } from '@playwright/test'

test.describe('Session persistence (AUTH-05)', () => {
  test.skip('authenticated user does not need to re-login after page reload', async () => {})
  test.skip('session cookie is set as httpOnly after successful login', async () => {})
})
