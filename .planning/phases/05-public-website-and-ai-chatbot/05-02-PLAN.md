---
phase: 05-public-website-and-ai-chatbot
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/[locale]/page.tsx
  - app/[locale]/(marketing)/about/page.tsx
  - app/[locale]/(marketing)/learn-pickleball/page.tsx
  - app/[locale]/(marketing)/events/page.tsx
  - app/[locale]/(marketing)/contact/page.tsx
  - app/[locale]/(marketing)/layout.tsx
  - components/public/ValueTimeline.tsx
  - components/public/TableOfContents.tsx
  - components/public/EventCard.tsx
  - components/public/CourtDiagram.tsx
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
  - PUB-05
must_haves:
  truths:
    - "Visitor can navigate to Home, About, Learn Pickleball, Events, and Contact pages in both Spanish and English"
    - "Home page hero, features, plans, and CTA sections render CMS content from content_blocks"
    - "About page displays vision, mission, and 6 values in a vertical timeline with scroll reveals"
    - "Learn Pickleball page shows origin, rules, scoring, equipment, and court diagram with sticky TOC"
    - "Events page displays upcoming tournaments, training, and social events in a card grid with colored type badges"
    - "Contact page shows WhatsApp CTA, club phone, email, and social handles"
    - "All pages have bilingual SEO metadata (title, description, OG tags, hreflang alternates)"
  artifacts:
    - path: "app/[locale]/page.tsx"
      provides: "Homepage rewired to CMS content_blocks"
      contains: "getContentBlocks"
    - path: "app/[locale]/(marketing)/about/page.tsx"
      provides: "About page with vision, mission, values timeline"
      contains: "about_"
    - path: "app/[locale]/(marketing)/learn-pickleball/page.tsx"
      provides: "Learn page with sticky TOC and CMS sections"
      contains: "learn_"
    - path: "app/[locale]/(marketing)/events/page.tsx"
      provides: "Events page with card grid from database"
      contains: "events"
    - path: "app/[locale]/(marketing)/contact/page.tsx"
      provides: "Contact page with WhatsApp CTA"
      contains: "wa.me"
    - path: "app/[locale]/(marketing)/layout.tsx"
      provides: "Marketing layout with MotionProvider, Footer, WhatsApp bubble"
    - path: "components/public/ValueTimeline.tsx"
      provides: "Vertical timeline for About page values"
    - path: "components/public/TableOfContents.tsx"
      provides: "Sticky TOC with scroll spy for Learn page"
    - path: "components/public/EventCard.tsx"
      provides: "Event card with type badge and image/icon fallback"
  key_links:
    - from: "app/[locale]/page.tsx"
      to: "lib/content.ts"
      via: "getContentBlocks('home_', locale)"
      pattern: "getContentBlocks.*home_"
    - from: "app/[locale]/(marketing)/events/page.tsx"
      to: "supabase events table"
      via: "createClient().from('events').select()"
      pattern: "from.*events.*select"
    - from: "app/[locale]/(marketing)/layout.tsx"
      to: "components/motion/MotionProvider.tsx"
      via: "wraps children in LazyMotion context"
      pattern: "MotionProvider"
---

<objective>
Create all five public pages (Home rewire, About, Learn Pickleball, Events, Contact) as Server Components with CMS content, Framer Motion scroll reveals, and bilingual SEO metadata. Create the marketing layout wrapper with MotionProvider, Footer, and WhatsApp bubble.

Purpose: These pages are the member acquisition layer -- visitors land here, learn about the club, and convert to signups.
Output: Five fully functional bilingual public pages with CMS-driven content and SEO.
</objective>

<execution_context>
@/Users/joseurizar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseurizar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-public-website-and-ai-chatbot/05-CONTEXT.md
@.planning/phases/05-public-website-and-ai-chatbot/05-RESEARCH.md
@.planning/phases/05-public-website-and-ai-chatbot/05-01-SUMMARY.md

@app/[locale]/page.tsx
@lib/types/admin.ts
@components/Navbar.tsx

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From lib/content.ts:
```typescript
export async function getContentBlocks(prefix: string, locale: string): Promise<Record<string, string>>
export async function getContentBlock(key: string, locale: string): Promise<string>
```

From components/motion/MotionProvider.tsx:
```typescript
// "use client" - wraps children in LazyMotion with domAnimation
export function MotionProvider({ children }: { children: ReactNode }): JSX.Element
```

From components/motion/ScrollReveal.tsx:
```typescript
// "use client" - viewport-triggered fade-in wrapper
export function ScrollReveal({ children, delay, className }: {
  children: ReactNode; delay?: number; className?: string
}): JSX.Element
```

From components/motion/StaggerChildren.tsx:
```typescript
// "use client" - staggered entrance orchestrator
export function StaggerChildren({ children, staggerDelay, className }: {
  children: ReactNode; staggerDelay?: number; className?: string
}): JSX.Element
```

From components/motion/HeroEntrance.tsx:
```typescript
// "use client" - hero text entrance animation on mount
export function HeroEntrance({ children, className }: {
  children: ReactNode; className?: string
}): JSX.Element
```

From components/Footer.tsx:
```typescript
// Server Component - public footer with nav, social, WhatsApp, email
export async function Footer(): Promise<JSX.Element>
```

From components/public/WhatsAppBubble.tsx:
```typescript
// "use client" - floating WhatsApp button bottom-left
export function WhatsAppBubble({ locale }: { locale: string }): JSX.Element
```

From lib/types/admin.ts:
```typescript
export type EventType = 'tournament' | 'training' | 'social'
export interface Event {
  id: string; title_es: string; title_en: string;
  description_es: string | null; description_en: string | null;
  event_date: string; location_id: string | null;
  event_type: EventType; start_time: string | null;
  end_time: string | null; image_url: string | null;
  created_at: string;
}
```

Seeded content_block keys:
- home_hero, home_overview
- about_vision, about_mission, about_values
- learn_origin, learn_rules, learn_scoring, learn_equipment, learn_court_dimensions
- contact_info, footer_social_links

Brand colors: lime (#39FF14), midnight (#0B1D3A), turquoise (#1ED6C3), sunset (#FF6B2C), offwhite (#F5F5F5), charcoal (#1A2744)
Fonts: Bebas Neue (headings), Inter (body), Poppins (accent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Marketing layout, Home page CMS rewire, About page, and Contact page</name>
  <files>
    app/[locale]/(marketing)/layout.tsx,
    app/[locale]/page.tsx,
    app/[locale]/(marketing)/about/page.tsx,
    app/[locale]/(marketing)/contact/page.tsx,
    components/public/ValueTimeline.tsx
  </files>
  <action>
    1. Create `app/[locale]/(marketing)/layout.tsx`:
       - Import MotionProvider, Footer, WhatsAppBubble
       - Wrap `{children}` in MotionProvider
       - Render Footer below children
       - Render WhatsAppBubble with locale prop
       - Get locale via `getLocale()` from `next-intl/server`

    2. Rewire `app/[locale]/page.tsx` (Homepage):
       - PRESERVE existing searchParams/welcome banner logic completely (per research Pitfall 7)
       - Import `getContentBlocks` from `@/lib/content`
       - Fetch `getContentBlocks('home_', locale)` where locale comes from `getLocale()`
       - Replace `t('heroHeadline')` etc. with CMS content where appropriate:
         - Hero section: keep existing layout/design but replace heading/subheading text with CMS `home_hero` content. Since CMS returns HTML, parse or use dangerouslySetInnerHTML for the overview section
         - Features section: keep the 3-card layout as-is -- these use i18n keys which is fine (they're UI chrome, not marketing copy). Per CONTEXT.md: "wire CMS content_blocks to replace i18n keys" -- but the feature cards are structural, so keep i18n for card titles/descriptions
         - Plans section: keep i18n keys (pricing data, not CMS content)
         - CTA section: replace heading/subheadline with CMS `home_overview` content or keep i18n
       - Wrap hero in HeroEntrance component
       - Wrap features and plans sections in ScrollReveal
       - Import MotionProvider and wrap the whole page content (homepage is outside marketing route group)
       - Import and render Footer at bottom
       - Import and render WhatsAppBubble
       - Contrast fix per CONTEXT.md: raise `text-offwhite/60` to `text-offwhite/70`, eliminate any opacity below 50%. Replace `text-offwhite/30` with brand color icons
       - Add `generateMetadata` function with bilingual title, description, OG tags, hreflang alternates
       - Use more vibrant Sunset Orange as dual accent per CONTEXT.md decision

    3. Create `components/public/ValueTimeline.tsx` ("use client"):
       - Vertical timeline layout for About page 6 values
       - Values: Love & Passion, Accessibility, Discipline, Respect, Social Commitment, Integrity
       - Each value: icon (SVG), title, description -- on alternating sides of a vertical line on desktop, stacked on mobile
       - Vertical connecting line in the center (lime gradient)
       - Each value uses ScrollReveal for staggered reveal as user scrolls
       - Props: `values: Array<{ icon: ReactNode; title: string; description: string }>` -- OR parse from CMS content
       - Dark theme styling: bg-midnight, text-offwhite, accent colors (lime, turquoise, sunset rotating)
       - Mobile: single column, timeline line on left side

    4. Create `app/[locale]/(marketing)/about/page.tsx` (RSC):
       - Fetch content blocks: `getContentBlocks('about_', locale)`
       - Hero section: club name, brief description
       - Vision section: render `about_vision` content with ScrollReveal
       - Mission section: render `about_mission` content with ScrollReveal
       - Values section: render `about_values` -- parse the 6 values and pass to ValueTimeline component
         - The 6 values (Love & Passion, Accessibility, Discipline, Respect, Social Commitment, Integrity) -- titles from i18n, descriptions from CMS
       - Bottom CTA: link to Pricing page
       - `generateMetadata` with bilingual SEO

    5. Create `app/[locale]/(marketing)/contact/page.tsx` (RSC):
       - Fetch `getContentBlock('contact_info', locale)` for club details
       - Primary CTA: large WhatsApp button with pre-filled greeting
       - Club info: phone number, email (nellpickleball@gmail.com)
       - Social handles: fetch from `footer_social_links` content_block, render Instagram/Facebook links with icons
       - Layout: centered, clean, dark theme
       - No contact form (per CONTEXT.md -- replaced by WhatsApp)
       - `generateMetadata` with bilingual SEO
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Marketing layout wraps pages with MotionProvider + Footer + WhatsApp. Homepage preserves welcome banner, rewired to CMS content with HeroEntrance and ScrollReveal animations, contrast fixes applied. About page renders vision/mission from CMS, values in vertical timeline. Contact page shows WhatsApp CTA, phone, email, social links. All three pages have generateMetadata with bilingual SEO.</done>
</task>

<task type="auto">
  <name>Task 2: Learn Pickleball page, Events page, and supporting components</name>
  <files>
    app/[locale]/(marketing)/learn-pickleball/page.tsx,
    app/[locale]/(marketing)/events/page.tsx,
    components/public/TableOfContents.tsx,
    components/public/EventCard.tsx,
    components/public/CourtDiagram.tsx
  </files>
  <action>
    1. Create `components/public/TableOfContents.tsx` ("use client"):
       - Sticky sidebar TOC for Learn Pickleball page
       - Props: `sections: Array<{ id: string; label: string }>`
       - Desktop (lg+): sticky sidebar on the left, `position: sticky; top: 6rem`
       - Mobile: collapsible dropdown/accordion at top of page
       - Scroll spy: highlight active section using IntersectionObserver
         - Observe each section element by ID
         - Update active state as user scrolls
       - Click handler: smooth scroll to section via `element.scrollIntoView({ behavior: 'smooth' })`
       - Dark theme: bg-charcoal/80 backdrop-blur, active item highlighted with lime left border
       - AnimatePresence for mobile accordion open/close

    2. Create `components/public/CourtDiagram.tsx`:
       - SVG court diagram showing pickleball court dimensions
       - Labels: 20' x 44' overall, 7' non-volley zone (kitchen), net at center
       - Color scheme: lime lines on midnight background, turquoise for kitchen zone
       - Responsive: scales with container width
       - Simple static SVG, no animation needed

    3. Create `app/[locale]/(marketing)/learn-pickleball/page.tsx` (RSC):
       - Fetch `getContentBlocks('learn_', locale)`
       - Layout: two-column on desktop (TOC sidebar + content), single-column on mobile
       - Sections with IDs for scroll spy:
         a. Origin (learn_origin) -- history of pickleball (1965, Bainbridge Island)
         b. Rules (learn_rules) -- basic gameplay rules
         c. Scoring (learn_scoring) -- scoring system
         d. Court Dimensions (learn_court_dimensions) -- include CourtDiagram SVG component
         e. Equipment (learn_equipment) -- paddles, balls, shoes
       - Each section wrapped in ScrollReveal
       - Section headings use Bebas Neue, large, with lime accent
       - CMS content rendered via dangerouslySetInnerHTML (admin-only editable, safe per research)
       - Pass section IDs and labels to TableOfContents component
       - `generateMetadata` with bilingual SEO

    4. Create `components/public/EventCard.tsx`:
       - Props: event object (Event type from admin.ts) + locale string
       - Card layout: rounded-2xl, bg-charcoal, border-charcoal
       - Top: image if `image_url` exists, otherwise icon fallback by event_type:
         - tournament: trophy icon (sunset color)
         - training: target/bullseye icon (turquoise)
         - social: users/party icon (lime)
       - Type badge: colored pill at top-right of image area
         - Tournament: bg-sunset text
         - Training: bg-turquoise text
         - Social: bg-lime text-midnight
       - Title: `title_es` or `title_en` based on locale
       - Description: truncated to 2-3 lines
       - Date + time display formatted for locale
       - Hover: lift slightly (scale + shadow) via CSS transition (not Framer Motion for cards)

    5. Create `app/[locale]/(marketing)/events/page.tsx` (RSC):
       - Fetch upcoming events from Supabase:
         ```
         supabase.from('events')
           .select('id, title_es, title_en, description_es, description_en, event_date, event_type, start_time, end_time, image_url')
           .gte('event_date', new Date().toISOString().split('T')[0])
           .order('event_date', { ascending: true })
         ```
       - Hero section: heading "Events" / "Eventos" with ScrollReveal
       - If no events: show friendly empty state message ("No upcoming events" / "No hay eventos proximos") with link to contact page
       - Event grid: 1-col mobile, 2-col desktop (md:grid-cols-2) gap-6
       - Wrap grid in StaggerChildren for staggered card entrance
       - Each event rendered as EventCard component
       - Bottom: link to Pricing page ("Join to participate" / "Unete para participar")
       - `generateMetadata` with bilingual SEO
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Learn Pickleball page renders 5 CMS sections with sticky TOC (scroll spy on desktop, accordion on mobile) and SVG court diagram. Events page fetches upcoming events from database and displays in 2-col card grid with colored type badges, staggered entrance animation, and empty state. Both pages have generateMetadata with bilingual SEO.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- All 5 public pages exist and are accessible at their routes
- Homepage preserves welcome banner functionality
- Each page has generateMetadata with bilingual title, description, OG, hreflang
- Content blocks are fetched from Supabase (not hardcoded)
- Events page queries the events table for upcoming events
- Motion animations render on public pages (scroll reveals, hero entrance, staggered cards)
</verification>

<success_criteria>
- Home page renders CMS content with hero entrance animation and scroll reveals
- About page shows vision, mission, and 6 values in vertical timeline with staggered scroll reveals
- Learn Pickleball page has sticky TOC (desktop) / accordion (mobile) with 5 CMS sections and SVG court diagram
- Events page displays upcoming events in 2-col grid with colored type badges (orange/turquoise/lime)
- Contact page shows WhatsApp CTA, phone, email, social handles -- no contact form
- All text from CMS content_blocks or i18n (no hardcoded marketing copy)
- All pages have bilingual SEO metadata with hreflang alternates
- Contrast issues fixed: nothing below 50% opacity, icons use brand colors
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-website-and-ai-chatbot/05-02-SUMMARY.md`
</output>
