---
phase: 03-reservations
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - "app/[locale]/(member)/reservations/page.tsx"
  - "app/[locale]/(member)/reservations/CourtCard.tsx"
  - "app/[locale]/(member)/reservations/TimeSlotGrid.tsx"
  - "app/[locale]/(member)/reservations/CourtDiagram.tsx"
  - lib/queries/reservations.ts
  - components/Navbar.tsx
  - messages/en.json
  - messages/es.json
autonomous: true
requirements: [RESV-01, RESV-07, MAP-01, MAP-02, MAP-03, MAP-05]

must_haves:
  truths:
    - "User sees 3 court cards on the reservation page with Google Maps thumbnails"
    - "Each card shows today's time slots with availability status"
    - "User can switch between Today, Tomorrow, +2, +3 days via tab strip"
    - "Clicking a time slot opens a court diagram modal showing 4 quadrant spots"
    - "Available spots show in Electric Lime green, taken spots show in red"
    - "Open play mode shows individual spot availability, full court mode shows single Book Court button"
  artifacts:
    - path: "app/[locale]/(member)/reservations/page.tsx"
      provides: "Server Component fetching courts, configs, and reservations for availability"
      min_lines: 30
    - path: "app/[locale]/(member)/reservations/CourtCard.tsx"
      provides: "Client Component rendering one court card with Google Maps thumbnail and time slot grid"
      min_lines: 50
    - path: "app/[locale]/(member)/reservations/TimeSlotGrid.tsx"
      provides: "Client Component with tab strip date selector and time slot table"
      min_lines: 40
    - path: "app/[locale]/(member)/reservations/CourtDiagram.tsx"
      provides: "Client Component showing 4-quadrant court diagram in modal"
      min_lines: 40
    - path: "lib/queries/reservations.ts"
      provides: "Server-side query functions for court availability"
      exports: ["getCourtAvailability", "getTimeSlots"]
  key_links:
    - from: "app/[locale]/(member)/reservations/page.tsx"
      to: "lib/queries/reservations.ts"
      via: "server-side data fetching"
      pattern: "getCourtAvailability"
    - from: "app/[locale]/(member)/reservations/CourtCard.tsx"
      to: "TimeSlotGrid.tsx"
      via: "component composition"
      pattern: "TimeSlotGrid"
    - from: "app/[locale]/(member)/reservations/TimeSlotGrid.tsx"
      to: "CourtDiagram.tsx"
      via: "modal trigger on slot click"
      pattern: "CourtDiagram"
---

<objective>
Court cards reservation page: 3 court cards with Google Maps thumbnails, time slot grid with tab strip date selector, and court diagram modal showing spot availability with green/red quadrant nodes.

Purpose: This is the primary UI members interact with to view court availability and select a time slot for booking. It is the visual core of the reservation system.
Output: Reservation page with court cards, time slot grid, court diagram modal, availability query functions
</objective>

<execution_context>
@/Users/joseurizar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseurizar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-reservations/03-CONTEXT.md
@.planning/phases/03-reservations/03-RESEARCH.md
@.planning/phases/03-reservations/03-01-SUMMARY.md

@lib/types/reservations.ts
@lib/supabase/server.ts
@components/Navbar.tsx

<interfaces>
<!-- Types created by Plan 03-01 -->

From lib/types/reservations.ts (created in 03-01):
```typescript
export type BookingMode = 'full_court' | 'open_play'
export type PaymentStatus = 'free' | 'pending_payment' | 'paid' | 'cash_pending'
export type ReservationStatus = 'confirmed' | 'pending_payment' | 'paid' | 'cancelled' | 'expired'
export interface TimeSlot { startTime: string; endTime: string; mode: BookingMode; spots: SpotInfo[] }
export interface SpotInfo { spotNumber: number; isAvailable: boolean; reservedBy?: string }
export interface CourtWithConfig { /* court + config + pricing combined */ }
```

From lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

Established patterns:
- Brand colors: Electric Lime #39FF14, Midnight Blue #0B1D3A, Caribbean Turquoise #1ED6C3, Sunset Orange #FF6B2C
- Typography: Bebas Neue (headings), Inter (body), Poppins (accent)
- next-intl useTranslations() works in both RSC and Client Components (next-intl 4)
- Server Components fetch data, Client Components handle interactivity
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Availability queries + Reservation page Server Component + Navbar link</name>
  <files>lib/queries/reservations.ts, app/[locale]/(member)/reservations/page.tsx, components/Navbar.tsx, messages/en.json, messages/es.json</files>
  <action>
1. Create `lib/queries/reservations.ts` with server-side query functions:

`getCourtAvailability(date: string)`: Fetches all courts with their court_config (for the date's day type -- weekday/weekend), court_pricing, and existing reservations for the given date. Returns `CourtWithConfig[]` where each court has its schedule, pricing, and current reservations for that date.

The function should:
- Query `courts` joined with `locations` (for name, address)
- Query `court_config` filtered by day_type (determine from date: Sat/Sun = weekend, else weekday)
- Query `reservations` for the target date where status NOT IN ('cancelled', 'expired') -- include 'pending_payment' as occupied per Pitfall 4
- For pending_payment reservations, check if hold has expired: `created_at + hold_window < NOW()`. Expired holds should NOT count as occupied (on-demand check per RESEARCH discretion recommendation)
- Query `app_config` for hold duration
- Return structured data with courts, their configs, and per-slot availability

`generateTimeSlots(config: CourtConfig, date: string, reservations: Reservation[])`: Pure function that generates an array of `TimeSlot` objects from the court config hours, checking each 1-hour slot against existing reservations. For open play slots, populate SpotInfo for spots 1-4. For full court slots, indicate if the court is booked or not.

2. Create `app/[locale]/(member)/reservations/page.tsx` as a Server Component:
- Authenticate user via supabase.auth.getUser()
- If not authenticated, redirect to /login
- Fetch user's membership status (to determine if they book free or need payment)
- Call getCourtAvailability for today's date
- Fetch app_config for advance booking windows
- Determine user's max advance booking days (member vs non-member)
- Pass all data as props to the client components (CourtCard for each court)
- Use next-intl useTranslations for page title and labels
- Page layout: title "Reserve a Court" / "Reserva una Cancha", then 3 court cards in a responsive grid (1 col mobile, 2 col md, 3 col lg)

3. Update `components/Navbar.tsx`: Add "Reservations" / "Reservas" link visible to all authenticated users (not gated by membership). Use the Reservations i18n namespace.

4. Add Reservations i18n namespace to `messages/en.json` and `messages/es.json`:
- Page title, court card labels, time slot labels, mode labels (Full Court / Open Play), spot availability text, date tab labels (Today, Tomorrow, etc.), booking button text, no availability text, pricing info text ("Free for members" / "$10 per session"), Google Maps link text
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Reservation page renders with court data from server, availability queries compute correct slot status including expired hold filtering, Navbar shows reservations link, i18n strings in both languages.</done>
</task>

<task type="auto">
  <name>Task 2: Court cards, time slot grid, and court diagram modal</name>
  <files>app/[locale]/(member)/reservations/CourtCard.tsx, app/[locale]/(member)/reservations/TimeSlotGrid.tsx, app/[locale]/(member)/reservations/CourtDiagram.tsx</files>
  <action>
1. Create `CourtCard.tsx` ('use client'):
- Receives court data (name, location, lat/lng, config, pricing, availability) as props
- Google Maps Static API thumbnail at top of card: construct URL `https://maps.googleapis.com/maps/api/staticmap?center={lat},{lng}&zoom=16&size=400x200&markers=color:green|{lat},{lng}&key={NEXT_PUBLIC_GOOGLE_MAPS_API_KEY}`. Use `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` env var. If key not set, show a placeholder div with court name instead (graceful fallback per MAP-05).
- Click on thumbnail opens Google Maps directions: `https://www.google.com/maps/dir/?api=1&destination={lat},{lng}`
- Card header: court name, location/address, today's operating hours from config
- Pricing info: "$10 per session / Free for members" (from court_pricing + membership status passed as prop)
- Current mode indicator badge (Full Court or Open Play based on current hour's config)
- Render `<TimeSlotGrid>` component below the header
- Tailwind styling: rounded card with shadow, Midnight Blue border accent, responsive width

2. Create `TimeSlotGrid.tsx` ('use client'):
- Receives: time slots array, selected date, available dates (based on advance booking window), court info, user membership status
- Horizontal tab strip at top: Today, Tomorrow, +2 days, +3 days (or fewer based on advance booking window). Non-members only see Today tab (24hr window). Members see up to 3 days ahead.
- When tab changes, call a passed `onDateChange(date)` callback that the parent page uses to re-fetch availability (via React state + Server Action or client-side fetch)
- Time slot table: rows for each 1-hour slot within the court's operating hours
- Each row shows: time range (e.g., "6:00 PM - 7:00 PM" in America/Santo_Domingo), mode badge (Full Court / Open Play), availability indicator
- For full court slots: show "Available" green badge or "Booked" red badge, with a "Book Court" button if available
- For open play slots: show "X/4 spots" with green/red dot indicators, with a "View Spots" button that opens the court diagram modal
- Use `Intl.DateTimeFormat('en-US', { timeZone: 'America/Santo_Domingo', ... })` for time display (RESV-04)
- Clicking "Book Court" or selecting a spot in diagram triggers a booking form (the actual booking action is in Plan 03-03; for now, emit an event/callback with selected slot + spot info)

3. Create `CourtDiagram.tsx` ('use client'):
- Modal component (triggered from TimeSlotGrid "View Spots" button)
- Shows an abstract 2x2 quadrant layout representing 4 court spots (per RESEARCH discretion: abstract layout over realistic doubles positions)
- Each quadrant is a div/button:
  - Available spot: Electric Lime (#39FF14) background, clickable, shows "Spot {N}" label
  - Taken spot: Red/coral background, not clickable, shows reserved user's first name (from reservation snapshot) or "Occupied"
- Per-time-slot navigation: user can browse time slots within the modal to see occupancy for each
- VIP guest: if user is VIP, show a "Add Guest" text input under the selected spot (guest_name field) -- only visible for VIP plan members
- "Reserve Spot" / "Reservar" button at bottom of modal -- collects: courtId, date, timeSlot, spotNumber, guestName (optional). Passes data up to parent for booking action.
- Close button (X) in modal corner
- Modal backdrop click to dismiss
- Brand styling: Midnight Blue background for modal header, Inter body font
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>3 court cards render on reservation page with Google Maps thumbnails (or graceful fallback), time slot grid shows availability per hour with date tab navigation, court diagram modal shows 4 quadrant spots with green/red color coding and VIP guest name input.</done>
</task>

</tasks>

<verification>
- Reservation page at /[locale]/reservations renders 3 court cards
- Each card shows Google Maps thumbnail (or placeholder if no API key)
- Time slots reflect correct operating hours from court_config
- Tab strip shows correct number of days based on user's booking window
- Court diagram modal opens and shows 4 quadrant spots
- All text is bilingual via i18n namespace
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Court cards display availability data from database
- Time slot grid correctly computes open play vs full court mode per hour
- Availability includes pending_payment reservations as occupied (expired holds excluded)
- Court diagram visually distinguishes available (green) and taken (red) spots
- Non-members see only Today tab; members see up to 3 days ahead
</success_criteria>

<output>
After completion, create `.planning/phases/03-reservations/03-02-SUMMARY.md`
</output>
