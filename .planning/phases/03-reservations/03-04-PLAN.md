---
phase: 03-reservations
plan: "04"
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - "app/[locale]/(member)/reservations/ReservationForm.tsx"
  - "app/[locale]/(member)/reservations/PaymentPanel.tsx"
  - "app/[locale]/(member)/reservations/page.tsx"
  - "app/[locale]/(member)/dashboard/page.tsx"
  - "app/[locale]/(member)/dashboard/ReservationsTable.tsx"
  - "app/[locale]/(member)/dashboard/CancelDialog.tsx"
  - messages/en.json
  - messages/es.json
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "User can complete a booking from the court card by clicking Reserve and confirming"
    - "Non-member user sees payment options (Stripe or cash) after reserving"
    - "Dashboard shows upcoming reservations in a table with cancel option"
    - "Cancel button shows confirmation dialog with reservation details"
    - "Cancellation within window succeeds; outside window shows informative error"
  artifacts:
    - path: "app/[locale]/(member)/reservations/ReservationForm.tsx"
      provides: "Client Component form that calls createReservationAction"
      min_lines: 30
    - path: "app/[locale]/(member)/reservations/PaymentPanel.tsx"
      provides: "Client Component showing payment options for non-members after booking"
      min_lines: 20
    - path: "app/[locale]/(member)/dashboard/ReservationsTable.tsx"
      provides: "Client Component displaying upcoming reservations with cancel buttons"
      min_lines: 40
    - path: "app/[locale]/(member)/dashboard/CancelDialog.tsx"
      provides: "Confirmation modal for reservation cancellation"
      min_lines: 20
  key_links:
    - from: "ReservationForm.tsx"
      to: "app/actions/reservations.ts"
      via: "useActionState calling createReservationAction"
      pattern: "createReservationAction"
    - from: "PaymentPanel.tsx"
      to: "app/actions/sessionPayment.ts"
      via: "form calling createSessionPaymentAction"
      pattern: "createSessionPaymentAction"
    - from: "CancelDialog.tsx"
      to: "app/actions/reservations.ts"
      via: "form calling cancelReservationAction"
      pattern: "cancelReservationAction"
    - from: "dashboard/page.tsx"
      to: "supabase reservations query"
      via: "server-side fetch of upcoming reservations"
      pattern: "from.*reservations.*select"
---

<objective>
Wire the booking UI to Server Actions and build the dashboard reservations experience: reservation form connecting court diagram to createReservationAction, payment panel for non-members, dashboard reservations table with cancellation dialog.

Purpose: Connects the visual UI (Plan 03-02) to the business logic (Plan 03-03) and gives members a way to manage their bookings from the dashboard.
Output: ReservationForm, PaymentPanel, ReservationsTable, CancelDialog components; enhanced dashboard page
</objective>

<execution_context>
@/Users/joseurizar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseurizar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-reservations/03-CONTEXT.md
@.planning/phases/03-reservations/03-01-SUMMARY.md
@.planning/phases/03-reservations/03-02-SUMMARY.md
@.planning/phases/03-reservations/03-03-SUMMARY.md

@app/[locale]/(member)/reservations/page.tsx
@app/[locale]/(member)/reservations/CourtDiagram.tsx
@app/[locale]/(member)/dashboard/page.tsx
@app/[locale]/(member)/dashboard/MembershipCard.tsx
@app/actions/reservations.ts
@app/actions/sessionPayment.ts
@lib/types/reservations.ts

<interfaces>
From app/actions/reservations.ts (created in 03-03):
```typescript
export async function createReservationAction(_prevState: unknown, formData: FormData): Promise<{ success?: boolean; reservationId?: string; needsPayment?: boolean; error?: string }>
export async function cancelReservationAction(_prevState: unknown, formData: FormData): Promise<{ success?: boolean; error?: string }>
```

From app/actions/sessionPayment.ts (created in 03-03):
```typescript
export async function createSessionPaymentAction(_prevState: unknown, formData: FormData): Promise<never> // redirects to Stripe
```

From app/[locale]/(member)/dashboard/MembershipCard.tsx (created in Phase 2):
```typescript
// Existing membership card showing active/cancelled/past_due states
// Dashboard page already fetches membership data
```

Pattern: React 19 useActionState hook for Server Action forms
- Signature: const [state, formAction, isPending] = useActionState(serverAction, initialState)
- Form: <form action={formAction}>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReservationForm + PaymentPanel + wire to court diagram</name>
  <files>app/[locale]/(member)/reservations/ReservationForm.tsx, app/[locale]/(member)/reservations/PaymentPanel.tsx, app/[locale]/(member)/reservations/CourtDiagram.tsx, app/[locale]/(member)/reservations/page.tsx</files>
  <action>
1. Create `ReservationForm.tsx` ('use client'):
- Uses React 19 `useActionState` with `createReservationAction`
- Hidden form fields: courtId, date, startTime, endTime, bookingMode, spotNumber
- For VIP users: guestName text input (only visible if user is VIP per prop)
- Submit button: "Reserve Spot" / "Reservar" with loading state (isPending)
- On success with `needsPayment: true`: show PaymentPanel component
- On success with `needsPayment: false` (member): show success message "Reservation confirmed! Check your email."
- On error: display error message using i18n key from the action response
- Style: compact form that fits inside the court diagram modal or as an inline panel

2. Create `PaymentPanel.tsx` ('use client'):
- Shown after a non-member creates a reservation (status: pending_payment)
- Receives: reservationId, priceCents
- Two options displayed as buttons:
  - "Pay with Stripe (${price})" -- form with useActionState calling createSessionPaymentAction with reservationId as hidden field. Redirects to Stripe Checkout.
  - "Pay Cash at Location" -- informative text: "Your reservation is held for {holdHours} hours. Please pay at the front desk before your session." Sets local state to show confirmation message.
- Price displayed formatted (e.g., "$10.00")
- Per RESEARCH discretion: inline panel on the reservation page (not a separate confirmation page)

3. Update `CourtDiagram.tsx`:
- Integrate ReservationForm inside the modal
- When user clicks an available spot (green quadrant), select it and show the ReservationForm below the diagram
- For full court mode: show ReservationForm directly (no spot selection needed)
- Pass courtId, date, startTime, endTime, bookingMode, spotNumber to the form

4. Update `reservations/page.tsx`:
- Pass user membership status (isMember, isVip) down to CourtCard -> TimeSlotGrid -> CourtDiagram -> ReservationForm
- Add `revalidatePath` or router.refresh() pattern after successful booking to update availability display
- Handle `?paid=true` and `?payment_cancelled=true` query params (from Stripe redirect) with appropriate toast/banner messages
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Users can click a spot in the court diagram, fill the reservation form, and submit. Members see instant confirmation. Non-members see PaymentPanel with Stripe and cash options. Stripe payment redirects to Checkout. Return from Stripe shows success banner.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard reservations table + cancellation dialog</name>
  <files>app/[locale]/(member)/dashboard/page.tsx, app/[locale]/(member)/dashboard/ReservationsTable.tsx, app/[locale]/(member)/dashboard/CancelDialog.tsx, messages/en.json, messages/es.json</files>
  <action>
1. Create `ReservationsTable.tsx` ('use client'):
- Receives: reservations array (upcoming, sorted by date ASC), cancellationWindowHours
- Simple table per CONTEXT.md: Date | Time | Court | Status | Cancel
- Date and time displayed in America/Santo_Domingo timezone using Intl.DateTimeFormat
- Status column shows badge: "Confirmed" (green), "Pending Payment" (yellow), "Paid" (green), "Cancelled" (gray)
- Cancel column: "Cancel" button (red text) for each reservation
  - Button disabled if reservation starts_at - now < cancellationWindowHours (show tooltip "Cancellation window has passed")
  - Button opens CancelDialog modal
- Empty state: "No upcoming reservations" with link to /reservations page
- For pending_payment reservations: show a "Pay Now" link that triggers the PaymentPanel flow (or links to /reservations?pay=reservationId)

2. Create `CancelDialog.tsx` ('use client'):
- Modal with reservation details: court name, date, time
- "Are you sure you want to cancel this reservation?" text
- Uses useActionState with cancelReservationAction
- Hidden field: reservationId
- Two buttons: "Yes, Cancel" (red, submits form) and "Keep Reservation" (closes modal)
- On success: close modal, parent refreshes data (via callback or router.refresh)
- On error: show error message (e.g., cancellation window passed -- race condition where window closed between page load and submit)

3. Update `dashboard/page.tsx`:
- Keep existing MembershipCard (DASH-01 -- already implemented in Phase 2)
- Fetch upcoming reservations: query reservations table where user_id = user.id AND starts_at > now AND status NOT IN ('cancelled', 'expired'), order by starts_at ASC
- Fetch cancellation_window_hours from app_config
- Render ReservationsTable below MembershipCard
- Layout: MembershipCard at top, then "Upcoming Reservations" section with ReservationsTable
- Add "Settings" link to /dashboard/settings (for Plan 03-05)

4. Add Dashboard.reservations i18n keys to messages/en.json and messages/es.json:
- Table headers, status badges, cancel button text, confirmation dialog text, empty state text, "Pay Now" text
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Dashboard shows membership card (existing) plus upcoming reservations table. Cancel button opens confirmation dialog. Cancellation calls Server Action and refreshes table. Pending payment reservations show Pay Now option. All text bilingual.</done>
</task>

</tasks>

<verification>
- ReservationForm submits to createReservationAction with all required fields
- PaymentPanel shows Stripe and cash options for non-members
- Dashboard page shows membership card + upcoming reservations
- CancelDialog calls cancelReservationAction with proper error handling
- All timestamps display in America/Santo_Domingo timezone
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- End-to-end booking flow works: select spot -> reserve -> confirmation (member) or payment options (non-member)
- Dashboard displays all upcoming reservations sorted by date
- Cancellation works within window, shows clear error outside window
- Stripe per-session payment redirect works and return shows success
</success_criteria>

<output>
After completion, create `.planning/phases/03-reservations/03-04-SUMMARY.md`
</output>
