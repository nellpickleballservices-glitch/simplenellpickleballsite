---
phase: 01-fix-critical-performance-issues
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0007_admin_view_rpc_ratelimit_index.sql
  - lib/middleware/cookie-signing.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Postgres view admin_users_view exists and returns profiles joined with auth.users data"
    - "RPC function batch_reorder_content_blocks accepts JSONB array and updates sort_order atomically"
    - "chat_rate_limits table exists with session_id, message_count, window_start columns"
    - "Composite index on reservations(court_id, starts_at) exists"
    - "pg_cron job cleans stale rate limit rows every hour"
    - "Cookie signing utility can sign and verify payloads using HMAC-SHA256 via Web Crypto API"
  artifacts:
    - path: "supabase/migrations/0007_admin_view_rpc_ratelimit_index.sql"
      provides: "All database objects needed by subsequent plans"
      contains: "admin_users_view"
    - path: "lib/middleware/cookie-signing.ts"
      provides: "HMAC cookie sign/verify + membership cache get/set"
      exports: ["setMembershipCookie", "getMembershipFromCookie"]
  key_links:
    - from: "supabase/migrations/0007_admin_view_rpc_ratelimit_index.sql"
      to: "admin_users_view"
      via: "CREATE VIEW"
      pattern: "CREATE.*VIEW.*admin_users_view"
    - from: "lib/middleware/cookie-signing.ts"
      to: "crypto.subtle"
      via: "Web Crypto API HMAC"
      pattern: "crypto\\.subtle"
---

<objective>
Create all database objects (Postgres view, RPC function, rate limit table, composite index, pg_cron cleanup job) and the cookie signing utility needed by all subsequent plans.

Purpose: Foundation layer -- every other plan depends on these database objects or the cookie utility existing first.
Output: One migration file with all DB objects, one TypeScript utility for signed membership cookies.
</objective>

<execution_context>
@/Users/joseurizar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseurizar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-critical-performance-issues/01-CONTEXT.md
@.planning/phases/01-fix-critical-performance-issues/01-RESEARCH.md

@supabase/migrations/0004_pg_cron_reminder.sql (reference for pg_cron pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Postgres migration with view, RPC, rate limit table, index, and pg_cron job</name>
  <files>supabase/migrations/0007_admin_view_rpc_ratelimit_index.sql</files>
  <action>
Create migration file `supabase/migrations/0007_admin_view_rpc_ratelimit_index.sql` with ALL of the following in this order:

1. **admin_users_view** — A view in the `public` schema joining profiles with auth.users:
```sql
CREATE OR REPLACE VIEW admin_users_view AS
SELECT
  p.id,
  p.first_name,
  p.last_name,
  p.phone,
  p.created_at,
  u.email,
  u.last_sign_in_at,
  u.banned_until,
  u.raw_app_meta_data->>'role' AS role
FROM public.profiles p
JOIN auth.users u ON u.id = p.id;
```
Add safety comment: "Only queried via supabaseAdmin (service_role) which bypasses RLS."
REVOKE ALL from anon and authenticated roles. GRANT SELECT to service_role only.

2. **batch_reorder_content_blocks RPC** — A SECURITY DEFINER function accepting JSONB array of `{id, sort_order}` objects. Loops through `jsonb_array_elements(items)`, updates `content_blocks` SET `sort_order` and `updated_at = now()` WHERE `id` matches. Set `search_path = public`. REVOKE from PUBLIC, GRANT EXECUTE to service_role only.

3. **chat_rate_limits table** — Create table with columns: `session_id text PRIMARY KEY`, `message_count int NOT NULL DEFAULT 1`, `window_start timestamptz NOT NULL DEFAULT now()`. Create index `idx_chat_rate_limits_window` on `(window_start)`.

4. **Composite index on reservations** — `CREATE INDEX IF NOT EXISTS idx_reservations_court_starts ON reservations (court_id, starts_at);`

5. **pg_cron cleanup job** — Schedule hourly deletion of stale rate limit rows: `DELETE FROM public.chat_rate_limits WHERE window_start < now() - interval '2 hours'`. Use the same `cron.schedule` pattern as migration 0004. Name the job `cleanup-stale-rate-limits`.

Important: Use `IF NOT EXISTS` where possible. The view uses `CREATE OR REPLACE`. The function uses `CREATE OR REPLACE`.
  </action>
  <verify>
    <automated>npx supabase db lint --level warning 2>&1 | head -20 || echo "Lint not available — verify SQL syntax manually: cat supabase/migrations/0007_admin_view_rpc_ratelimit_index.sql"</automated>
  </verify>
  <done>Migration file exists with all 5 database objects. SQL syntax is valid. View joins profiles with auth.users, RPC handles batch reorder, rate limit table has correct schema, composite index created, pg_cron job scheduled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create cookie signing utility with HMAC-SHA256 via Web Crypto API</name>
  <files>lib/middleware/cookie-signing.ts, tests/unit/cookieSigning.test.ts</files>
  <behavior>
    - sign(payload) produces a base64 string
    - verify(payload, signature) returns true for valid signatures
    - verify(payload, wrongSignature) returns false
    - verify(tamperedPayload, originalSignature) returns false
    - getMembershipFromCookie returns null when cookie is missing
    - getMembershipFromCookie returns null when signature is invalid
    - getMembershipFromCookie returns null when TTL (5 minutes) has expired
    - getMembershipFromCookie returns MembershipCache when cookie is valid and within TTL
    - setMembershipCookie sets httpOnly, secure, sameSite=lax, maxAge=300, path=/
  </behavior>
  <action>
Create `lib/middleware/cookie-signing.ts` following the exact pattern from RESEARCH.md Pattern 3:

- Use `crypto.subtle` (Web Crypto API) for HMAC-SHA256. Do NOT import Node.js `crypto` module — Edge Runtime requirement.
- Secret from `process.env.MEMBERSHIP_COOKIE_SECRET`
- Cookie name: `nell_membership_cache`
- MembershipCache interface: `{ active: boolean; planType: string | null; cachedAt: number }`
- `sign(payload: string): Promise<string>` — HMAC-SHA256, returns base64
- `verify(payload: string, signature: string): Promise<boolean>` — compares signatures
- `setMembershipCookie(response: NextResponse, data: MembershipCache): Promise<void>` — sets cookie with payload.signature format, httpOnly, secure, sameSite lax, maxAge 300, path /
- `getMembershipFromCookie(request: NextRequest): Promise<MembershipCache | null>` — reads cookie, verifies signature, checks 5-minute TTL, returns null if invalid/expired/missing
- Export: `setMembershipCookie`, `getMembershipFromCookie`, `MembershipCache` type

Create `tests/unit/cookieSigning.test.ts`:
- Test the `sign` and `verify` functions directly (export them for testing)
- Mock `process.env.MEMBERSHIP_COOKIE_SECRET` with a test value
- Test TTL expiry by creating a cache entry with `cachedAt` set to 6 minutes ago
- Test tamper detection by modifying the payload after signing
- For cookie get/set tests, create lightweight mock objects for NextRequest/NextResponse cookies

Note: `crypto.subtle` is available globally in Node.js 20+ test environment. No polyfill needed.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/cookieSigning.test.ts</automated>
  </verify>
  <done>Cookie signing utility exports setMembershipCookie and getMembershipFromCookie. All tests pass: valid signing, tamper detection, TTL expiry, missing cookie handling.</done>
</task>

</tasks>

<verification>
- Migration file contains all 5 database objects (view, RPC, table, index, pg_cron)
- Cookie signing tests pass: `npx vitest run tests/unit/cookieSigning.test.ts`
- Full test suite still green: `npm test`
</verification>

<success_criteria>
- supabase/migrations/0007_admin_view_rpc_ratelimit_index.sql exists with valid SQL for all 5 objects
- lib/middleware/cookie-signing.ts exports setMembershipCookie, getMembershipFromCookie
- tests/unit/cookieSigning.test.ts passes all assertions
- npm test passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-critical-performance-issues/01-01-SUMMARY.md`
</output>
