---
phase: 03-signup-country-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0008_country_column.sql
  - lib/data/countries.ts
  - components/CountrySelect.tsx
  - tests/unit/countryData.test.ts
  - tests/unit/countrySelect.test.ts
autonomous: true
requirements: [UCLS-01, UCLS-02]
must_haves:
  truths:
    - "Country data file contains all 249 ISO 3166-1 countries with EN and ES names"
    - "Flag emoji is correctly computed from ISO alpha-2 code"
    - "CountrySelect renders a searchable dropdown with DR pinned at top"
    - "Search matches country names in both languages regardless of current locale"
    - "Database has country column on profiles with NOT NULL DEFAULT 'DO'"
    - "RLS policy prevents user self-update of country column"
  artifacts:
    - path: "supabase/migrations/0008_country_column.sql"
      provides: "Country column, is_tourist_price column, RLS policy, admin_users_view update"
      contains: "ALTER TABLE profiles ADD COLUMN country"
    - path: "lib/data/countries.ts"
      provides: "Static country data with code, nameEn, nameEs, flag"
      exports: ["countries", "countryByCode", "Country"]
      min_lines: 260
    - path: "components/CountrySelect.tsx"
      provides: "Reusable searchable country dropdown component"
      exports: ["CountrySelect"]
      min_lines: 80
    - path: "tests/unit/countryData.test.ts"
      provides: "Tests for country data completeness and flag generation"
    - path: "tests/unit/countrySelect.test.ts"
      provides: "Tests for search filtering and DR pinning logic"
  key_links:
    - from: "components/CountrySelect.tsx"
      to: "lib/data/countries.ts"
      via: "import { countries, Country }"
      pattern: "import.*countries.*from.*@/lib/data/countries"
---

<objective>
Create the database migration for country support and the reusable CountrySelect component with its backing data file.

Purpose: Establishes the foundation (schema + UI component) that Plan 02 wires into signup, OAuth, and admin flows.
Output: Migration SQL, static country data (249 countries, bilingual), searchable CountrySelect component, and unit tests.
</objective>

<execution_context>
@/Users/joseurizar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseurizar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-signup-country-collection/03-CONTEXT.md
@.planning/phases/03-signup-country-collection/03-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From supabase/migrations/0001_initial_schema.sql (existing RLS policy to replace):
```sql
CREATE POLICY "Users can update own profile" ON profiles FOR UPDATE TO authenticated
  USING ((SELECT auth.uid()) = id) WITH CHECK ((SELECT auth.uid()) = id);
```

From supabase/migrations/0007_admin_view_rpc_ratelimit_index.sql (existing view to update):
```sql
CREATE OR REPLACE VIEW admin_users_view AS
SELECT p.id, p.first_name, p.last_name, p.phone, p.created_at,
       u.email, u.last_sign_in_at, u.banned_until,
       u.raw_app_meta_data->>'role' AS role
FROM public.profiles p
JOIN auth.users u ON u.id = p.id;
```

Existing form input styling pattern (from SignupForm.tsx):
```
className="bg-charcoal text-offwhite border border-[#1E293B] focus:border-turquoise rounded-lg px-4 py-2.5 outline-none transition-colors"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration and country data file</name>
  <files>supabase/migrations/0008_country_column.sql, lib/data/countries.ts, tests/unit/countryData.test.ts</files>
  <behavior>
    - Test: countries array has exactly 249 entries (ISO 3166-1 count)
    - Test: every country has a 2-char uppercase code matching /^[A-Z]{2}$/
    - Test: every country has non-empty nameEn and nameEs strings
    - Test: every country has a flag emoji string (length >= 2 chars)
    - Test: codeToFlag('DO') produces the Dominican Republic flag emoji
    - Test: countryByCode.get('US') returns the US entry
    - Test: countryByCode.get('XX') returns undefined
    - Test: 'DO' entry has nameEn 'Dominican Republic' and nameEs 'Republica Dominicana'
  </behavior>
  <action>
    1. Create `supabase/migrations/0008_country_column.sql` with:
       - `ALTER TABLE profiles ADD COLUMN country CHAR(2) NOT NULL DEFAULT 'DO'` (handles existing users)
       - `ALTER TABLE reservations ADD COLUMN is_tourist_price BOOLEAN DEFAULT false` (for Phase 5)
       - Drop existing "Users can update own profile" policy on profiles
       - Recreate RLS policy that allows users to update own profile BUT prevents changing the country column. Use WITH CHECK that validates `country IS NOT DISTINCT FROM (SELECT p.country FROM profiles p WHERE p.id = (SELECT auth.uid()))`. This blocks user self-update of country while service_role (admin) bypasses RLS entirely.
       - `CREATE OR REPLACE VIEW admin_users_view` adding `p.country` to the SELECT list (keep all existing columns)
       - Re-grant permissions: `REVOKE ALL ON admin_users_view FROM anon, authenticated; GRANT SELECT ON admin_users_view TO service_role;`

    2. Create `lib/data/countries.ts` with:
       - Export `Country` interface: `{ code: string; nameEn: string; nameEs: string; flag: string }`
       - Helper function `codeToFlag(code: string): string` using Regional Indicator Symbol math: `String.fromCodePoint(...code.toUpperCase().split('').map(c => 0x1F1E5 + c.charCodeAt(0) - 64))`
       - Export `countries: Country[]` array with all 249 ISO 3166-1 alpha-2 countries. Include accurate English and Spanish names. Generate flag from `codeToFlag()`. Source: use the full ISO 3166-1 list. Key entries to verify: AF/Afghanistan/Afganistan, DO/Dominican Republic/Republica Dominicana, US/United States/Estados Unidos, DE/Germany/Alemania, JP/Japan/Japon, CN/China/China, BR/Brazil/Brasil.
       - Export `countryByCode: Map<string, Country>` for O(1) lookup

    3. Create `tests/unit/countryData.test.ts` with tests from the behavior block above.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/countryData.test.ts -x</automated>
  </verify>
  <done>Migration SQL ready to apply, country data file exports 249 countries with bilingual names and flag emojis, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CountrySelect searchable dropdown component</name>
  <files>components/CountrySelect.tsx, tests/unit/countrySelect.test.ts</files>
  <behavior>
    - Test: filtering with "dom" returns Dominican Republic in results (English match)
    - Test: filtering with "alem" returns Germany (Spanish match "Alemania")
    - Test: filtering with "united" returns United States and United Kingdom
    - Test: DR is always first in the sorted results regardless of search
    - Test: DR does not appear twice (not in pinned AND in filtered list)
    - Test: empty search returns all 249 countries with DR first
    - Test: search is case-insensitive
  </behavior>
  <action>
    Create `components/CountrySelect.tsx` as a 'use client' component:

    **Props interface:**
    ```typescript
    interface CountrySelectProps {
      name: string          // form field name (e.g. "country")
      label: string         // translated label from parent
      locale: 'en' | 'es'  // for display names
      defaultValue?: string // ISO code, defaults to 'DO'
    }
    ```

    **Component behavior (per locked decisions):**
    - Hidden `<input type="hidden" name={name} value={selected} />` carries value to FormData
    - Trigger button shows selected country flag + name in current locale, styled to match existing inputs: `bg-charcoal text-offwhite border border-[#1E293B] focus:border-turquoise rounded-lg px-4 py-2.5`
    - When open: search input at top of dropdown, results list below
    - Search filters countries matching in BOTH languages regardless of locale (typing "Alemania" or "Germany" both find Germany)
    - Results list: DR pinned at top with a visual separator (border-b), then all matching countries alphabetical in current locale
    - DR must NOT appear duplicated (filter it from alphabetical list)
    - Click on country: select it, close dropdown, update hidden input
    - Click outside: close dropdown (useEffect with mousedown listener on document, cleanup on unmount)
    - Keyboard: Escape closes dropdown
    - Default value: 'DO' (Dominican Republic pre-selected)
    - Full-width, matching existing form styling
    - Max-height on dropdown list with overflow-y-auto (use Claude's discretion for exact value, ~60-64 max-h recommended)
    - Dropdown positioned absolutely below trigger

    **Extract filtering/sorting logic into exported helper functions** so tests can validate without rendering:
    - `export function filterCountries(search: string): Country[]` — filters by both nameEn and nameEs
    - `export function sortWithDRFirst(countries: Country[], locale: 'en' | 'es'): Country[]` — pins DR, sorts rest alphabetically

    Create `tests/unit/countrySelect.test.ts` testing the exported helper functions (filterCountries, sortWithDRFirst) per the behavior block. These are pure function tests, no DOM rendering needed.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/countrySelect.test.ts -x</automated>
  </verify>
  <done>CountrySelect component renders a searchable country dropdown with flag emojis, bilingual search, DR pinned first with separator, click-outside-to-close, matching existing form styling. Helper function tests all green.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/unit/countryData.test.ts tests/unit/countrySelect.test.ts -x` -- all tests pass
2. `npx tsc --noEmit` -- no TypeScript errors
3. Migration SQL file exists at `supabase/migrations/0008_country_column.sql`
</verification>

<success_criteria>
- 249 countries with bilingual names and flag emojis in `lib/data/countries.ts`
- CountrySelect component with searchable dropdown, DR pinned, bilingual search
- Migration SQL adds country column (NOT NULL DEFAULT 'DO'), updates RLS policy, updates admin_users_view
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-signup-country-collection/03-01-SUMMARY.md`
</output>
