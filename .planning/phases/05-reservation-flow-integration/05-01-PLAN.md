---
phase: 05-reservation-flow-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/utils/pricing.ts
  - lib/types/pricing.ts
  - tests/unit/pricing.test.ts
  - supabase/migrations/0010_default_session_price.sql
autonomous: true
requirements: [RESV-01, RESV-02]

must_haves:
  truths:
    - "calculateSessionPrice returns base price for local non-members"
    - "calculateSessionPrice returns base + surcharge for tourist non-members"
    - "isTourist returns true for null country"
    - "isTourist returns true for non-DO country"
    - "isTourist returns false for DO country"
    - "Surcharge rounding uses Math.round on fractional cents"
    - "default_session_price_cents exists in app_config"
  artifacts:
    - path: "lib/utils/pricing.ts"
      provides: "calculateSessionPrice and isTourist pure functions"
      exports: ["calculateSessionPrice", "isTourist"]
    - path: "lib/types/pricing.ts"
      provides: "PriceCalculationInput and PriceCalculationResult types"
      contains: "PriceCalculationResult"
    - path: "tests/unit/pricing.test.ts"
      provides: "Unit tests for pricing functions"
      min_lines: 40
    - path: "supabase/migrations/0010_default_session_price.sql"
      provides: "default_session_price_cents in app_config"
      contains: "default_session_price_cents"
  key_links:
    - from: "lib/utils/pricing.ts"
      to: "lib/types/pricing.ts"
      via: "import types"
      pattern: "PriceCalculationInput|PriceCalculationResult"
---

<objective>
Create the pure pricing calculation functions and their unit tests using TDD, plus the database migration for the configurable default price.

Purpose: These pure functions are the foundation that all three integration points (user reservation, admin walk-in, CourtCard display) depend on. Building them first with tests ensures correctness before wiring.
Output: `calculateSessionPrice()`, `isTourist()` in lib/utils/pricing.ts, types in lib/types/pricing.ts, unit tests, and app_config migration.
</objective>

<execution_context>
@/Users/joseurizar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseurizar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reservation-flow-integration/05-CONTEXT.md
@.planning/phases/05-reservation-flow-integration/05-RESEARCH.md

<interfaces>
<!-- Existing types to extend -->

From lib/types/pricing.ts:
```typescript
export interface SessionPricing {
  id: string
  court_id: string
  day_of_week: number  // 0=Sunday, 6=Saturday
  price_cents: number
}
// DAY_NAMES_EN, DAY_NAMES_ES also exported
```

From lib/utils/pricingValidation.ts (pattern to follow):
```typescript
export function validateDayOfWeek(day: number): boolean
export function validatePriceCents(cents: number): boolean
export function validateSurchargePct(pct: number): boolean
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD pricing pure functions and types</name>
  <files>lib/utils/pricing.ts, lib/types/pricing.ts, tests/unit/pricing.test.ts</files>
  <behavior>
    - isTourist('DO') returns false
    - isTourist('US') returns true
    - isTourist(null) returns true (conservative per CONTEXT.md)
    - isTourist('') returns true
    - calculateSessionPrice({ basePriceCents: 1000, surchargePercent: 25, isTourist: false }) returns { totalCents: 1000, surchargeAmountCents: 0 }
    - calculateSessionPrice({ basePriceCents: 1000, surchargePercent: 25, isTourist: true }) returns { totalCents: 1250, surchargeAmountCents: 250 }
    - calculateSessionPrice({ basePriceCents: 750, surchargePercent: 25, isTourist: true }) returns { totalCents: 938, surchargeAmountCents: 188 } (Math.round on 187.5)
    - calculateSessionPrice({ basePriceCents: 0, surchargePercent: 25, isTourist: true }) returns { totalCents: 0, surchargeAmountCents: 0 } (member $0 case)
    - Result object always includes all fields: basePriceCents, surchargePercent, surchargeAmountCents, totalCents, isTourist
  </behavior>
  <action>
    RED: Write tests first in tests/unit/pricing.test.ts covering all behaviors above. Run tests -- they must fail.

    GREEN: Add PriceCalculationInput and PriceCalculationResult interfaces to lib/types/pricing.ts (append to existing file, do not remove existing types). Create lib/utils/pricing.ts with:
    - `calculateSessionPrice(input: PriceCalculationInput): PriceCalculationResult` -- surchargeAmountCents = isTourist ? Math.round(basePriceCents * surchargePercent / 100) : 0, totalCents = basePriceCents + surchargeAmountCents
    - `isTourist(country: string | null): boolean` -- returns country !== 'DO'
    Run tests -- they must pass.

    REFACTOR: No refactor needed for these simple functions.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/pricing.test.ts</automated>
  </verify>
  <done>All pricing unit tests pass. calculateSessionPrice and isTourist are exported pure functions with correct types.</done>
</task>

<task type="auto">
  <name>Task 2: Database migration for default session price</name>
  <files>supabase/migrations/0010_default_session_price.sql</files>
  <action>
    Create migration file that inserts `default_session_price_cents` with value `1000` (= $10.00) into the `app_config` table. Use INSERT ... ON CONFLICT DO NOTHING to be idempotent. This replaces the hardcoded 1000 fallback in createReservationAction.

    Pattern: follow existing app_config inserts from prior migrations (key/value pairs).
  </action>
  <verify>
    <automated>grep -q 'default_session_price_cents' supabase/migrations/0010_default_session_price.sql && echo "PASS: migration contains default_session_price_cents" || echo "FAIL: key not found"</automated>
  </verify>
  <done>Migration file exists with valid SQL. app_config has default_session_price_cents = 1000.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/pricing.test.ts` -- all tests pass
- `lib/utils/pricing.ts` exports calculateSessionPrice and isTourist
- `lib/types/pricing.ts` exports PriceCalculationInput and PriceCalculationResult
- Migration file is valid SQL
</verification>

<success_criteria>
- Pure functions tested and exported
- Types defined for downstream plan consumption
- Default price migration ready
- Zero new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/05-reservation-flow-integration/05-01-SUMMARY.md`
</output>
