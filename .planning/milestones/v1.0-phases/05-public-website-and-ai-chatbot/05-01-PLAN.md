---
phase: 05-public-website-and-ai-chatbot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - supabase/migrations/0006_footer_social_links.sql
  - lib/content.ts
  - components/motion/MotionProvider.tsx
  - components/motion/ScrollReveal.tsx
  - components/motion/StaggerChildren.tsx
  - components/motion/HeroEntrance.tsx
  - components/Footer.tsx
  - components/public/WhatsAppBubble.tsx
  - components/public/MobileNav.tsx
  - components/Navbar.tsx
  - messages/en.json
  - messages/es.json
autonomous: true
requirements:
  - PUB-06
must_haves:
  truths:
    - "Motion library is installed and LazyMotion provider wraps public pages"
    - "Footer renders on public pages with navigation links, social icons, WhatsApp link, and club email"
    - "WhatsApp bubble floats bottom-left on all public pages with pre-filled bilingual greeting"
    - "Navbar shows public page links (About, Learn, Events, Contact, Pricing) and hamburger menu on mobile"
    - "Content helper fetches content_blocks by prefix and returns key-value map"
  artifacts:
    - path: "lib/content.ts"
      provides: "getContentBlocks helper for public page data fetching"
      exports: ["getContentBlocks"]
    - path: "components/motion/MotionProvider.tsx"
      provides: "LazyMotion wrapper with domAnimation features"
    - path: "components/motion/ScrollReveal.tsx"
      provides: "Viewport-triggered fade-in/slide-up wrapper"
    - path: "components/Footer.tsx"
      provides: "Public footer with nav links, social, WhatsApp, email"
    - path: "components/public/WhatsAppBubble.tsx"
      provides: "Floating WhatsApp link button bottom-left"
    - path: "components/public/MobileNav.tsx"
      provides: "Hamburger slide-out menu for mobile"
  key_links:
    - from: "components/motion/ScrollReveal.tsx"
      to: "components/motion/MotionProvider.tsx"
      via: "uses m.div inside LazyMotion context"
      pattern: "m\\.div.*whileInView"
    - from: "components/Footer.tsx"
      to: "lib/content.ts"
      via: "fetches footer_social_links content block"
      pattern: "getContentBlocks.*footer"
    - from: "components/Navbar.tsx"
      to: "components/public/MobileNav.tsx"
      via: "renders MobileNav for hamburger on small screens"
      pattern: "MobileNav"
user_setup:
  - service: whatsapp
    why: "WhatsApp business link for customer inquiries"
    env_vars:
      - name: NEXT_PUBLIC_WHATSAPP_PHONE
        source: "Club WhatsApp business phone number (digits only, with country code, e.g. 18091234567)"
---

<objective>
Install dependencies (motion, openai), create shared infrastructure components for all public pages: content block fetching helper, Framer Motion wrappers (MotionProvider, ScrollReveal, StaggerChildren, HeroEntrance), Footer with navigation and social links, WhatsApp floating bubble, mobile hamburger navigation, and enhanced Navbar with public page links.

Purpose: Every public page in Plans 02 and 03 depends on these shared components. Building them first prevents duplication and ensures consistent patterns.
Output: Shared component library, content helper, motion animation wrappers, Footer, WhatsApp bubble, enhanced Navbar with mobile support.
</objective>

<execution_context>
@/Users/joseurizar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseurizar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-website-and-ai-chatbot/05-CONTEXT.md
@.planning/phases/05-public-website-and-ai-chatbot/05-RESEARCH.md

@lib/types/admin.ts
@components/Navbar.tsx
@lib/supabase/server.ts
@messages/en.json
@messages/es.json

<interfaces>
<!-- Key types the executor needs -->

From lib/types/admin.ts:
```typescript
export interface ContentBlock {
  id: string
  block_key: string
  block_type: string
  content_es: string | null
  content_en: string | null
  sort_order: number
  updated_at: string
}
```

From lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

Existing Navbar pattern: Server Component using `getTranslations`, `createClient`, checks `user` for conditional links.

Brand colors (from tailwind config):
- Electric Lime: #39FF14 (`lime`)
- Midnight Blue: #0B1D3A (`midnight`)
- Caribbean Turquoise: #1ED6C3 (`turquoise`)
- Sunset Orange: #FF6B2C (`sunset`)
- Offwhite: #F5F5F5 (`offwhite`)
- Charcoal: #1A2744 (`charcoal`)

Fonts: Bebas Neue (headings), Inter (body), Poppins (accent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create migration, and build content helper + motion wrappers</name>
  <files>
    package.json, package-lock.json,
    supabase/migrations/0006_footer_social_links.sql,
    lib/content.ts,
    components/motion/MotionProvider.tsx,
    components/motion/ScrollReveal.tsx,
    components/motion/StaggerChildren.tsx,
    components/motion/HeroEntrance.tsx
  </files>
  <action>
    1. Install dependencies: `npm install motion openai`

    2. Create migration `supabase/migrations/0006_footer_social_links.sql`:
       - Insert `footer_social_links` content_block (block_type: 'plain_text') with JSON content for social links:
         content_es/content_en: `{"instagram":"https://instagram.com/nellpickleball","facebook":"https://facebook.com/nellpickleball"}`
       - Insert `learn_court_dimensions` content_block (block_type: 'rich_text') with placeholder content for SVG court diagram section
       - Insert `contact_info` content_block with club email (nellpickleball@gmail.com) and phone
       - Use ON CONFLICT (block_key) DO NOTHING

    3. Create `lib/content.ts` with `getContentBlocks(prefix: string, locale: string)` function:
       - Uses `createClient` from `@/lib/supabase/server` (anon role -- RLS allows public SELECT)
       - Selects `block_key, block_type, {content_field}` WHERE block_key LIKE `{prefix}%` ORDER BY sort_order
       - Returns `Record<string, string>` map of block_key to content value
       - Export a second helper `getContentBlock(key: string, locale: string)` for single block fetch (Footer social links)

    4. Create motion wrapper components (all "use client"):

       a. `components/motion/MotionProvider.tsx`:
          - Wraps children in `<LazyMotion features={domAnimation} strict>`
          - Import from `motion/react`
          - Simple pass-through wrapper

       b. `components/motion/ScrollReveal.tsx`:
          - Props: `children`, `delay?: number`, `className?: string`
          - Uses `m.div` with `initial={{ opacity: 0, y: 30 }}`, `whileInView={{ opacity: 1, y: 0 }}`
          - `viewport={{ once: true, margin: '-50px' }}`
          - `transition={{ duration: 0.5, delay, ease: 'easeOut' }}`
          - Does NOT include its own LazyMotion -- relies on MotionProvider ancestor

       c. `components/motion/StaggerChildren.tsx`:
          - Props: `children`, `staggerDelay?: number` (default 0.1), `className?: string`
          - Parent `m.div` with `variants` for stagger orchestration
          - `whileInView="visible"`, `viewport={{ once: true }}`
          - Children wrapped in `m.div` with item variants (opacity 0->1, y 20->0)

       d. `components/motion/HeroEntrance.tsx`:
          - Props: `children`, `className?: string`
          - Fade + slide up with staggered children on initial mount (not scroll-triggered)
          - Uses `animate` instead of `whileInView` since hero is above the fold
          - Stagger delay: 0.15s between headline, subheadline, CTA
  </action>
  <verify>
    <automated>node -e "require('motion/react'); console.log('motion OK');" && node -e "require('openai'); console.log('openai OK');" && test -f lib/content.ts && test -f components/motion/MotionProvider.tsx && test -f components/motion/ScrollReveal.tsx && echo "All files exist"</automated>
  </verify>
  <done>motion and openai packages installed. Migration 0006 ready. Content helper exports getContentBlocks and getContentBlock. Four motion wrapper components created with correct "use client" directives and motion/react imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create Footer, WhatsApp bubble, MobileNav, and enhance Navbar</name>
  <files>
    components/Footer.tsx,
    components/public/WhatsAppBubble.tsx,
    components/public/MobileNav.tsx,
    components/Navbar.tsx,
    messages/en.json,
    messages/es.json
  </files>
  <action>
    1. Create `components/Footer.tsx` (Server Component):
       - Fetches `footer_social_links` content_block via `getContentBlock('footer_social_links', locale)`
       - Parses JSON to get social link URLs (instagram, facebook)
       - Layout: dark background (bg-midnight), max-w-6xl container
       - Three columns on desktop (1-col stacked on mobile):
         a. Brand: "NELL" in Bebas Neue + "Pickleball Club" + club email (nellpickleball@gmail.com)
         b. Quick Nav: links to Home, About, Learn Pickleball, Events, Contact, Pricing
         c. Social: Instagram icon + Facebook icon (SVG icons, brand colors -- lime/turquoise)
       - WhatsApp link in footer as well
       - Bottom bar: copyright text
       - Static footer (no animation per CONTEXT.md)
       - i18n: use `getTranslations('Footer')` for link labels

    2. Create `components/public/WhatsAppBubble.tsx` ("use client"):
       - Floating button fixed bottom-left (left-6 bottom-6)
       - Green WhatsApp icon (use standard WhatsApp SVG or circle with phone icon)
       - Opens `https://wa.me/{NEXT_PUBLIC_WHATSAPP_PHONE}?text={encoded_greeting}`
       - Greeting based on locale prop: Spanish default "Hola, tengo una pregunta sobre NELL Pickleball Club", English "Hello, I have a question about NELL Pickleball Club"
       - Fallback phone: use env var `NEXT_PUBLIC_WHATSAPP_PHONE`, default to empty string if not set
       - z-index: 40 (below chat bubble at 50)
       - Subtle pulse animation on mount, then static
       - target="_blank" rel="noopener noreferrer"

    3. Create `components/public/MobileNav.tsx` ("use client"):
       - Hamburger button (3 horizontal lines icon) visible on mobile (md:hidden)
       - On click: full-screen overlay slide-out panel from right
       - Panel contents: all nav links (About, Learn, Events, Contact, Pricing, Dashboard if logged in, Admin if admin), LanguageSwitcher, Login/Signup buttons
       - Close button (X) at top-right of panel
       - Use `motion/react` AnimatePresence for enter/exit animation (slide from right)
       - Backdrop with semi-transparent dark overlay
       - Body scroll lock when open
       - Props: `user` (User | null), `firstName` (string | null), `isAdmin` (boolean)

    4. Enhance `components/Navbar.tsx`:
       - Add public page links visible to everyone (not just authenticated users): About, Learn Pickleball, Events, Contact
       - These links go BEFORE the Pricing link in desktop nav
       - Desktop nav: show links as `hidden md:flex` items
       - Mobile: hide all links (md:hidden), show MobileNav hamburger component instead
       - Add sticky behavior: the Navbar already has `w-full` -- add `sticky top-0 z-50 backdrop-blur-md` classes
       - Pass user/firstName/isAdmin props to MobileNav
       - Add i18n keys for new public page link labels

    5. Add i18n keys to messages/en.json and messages/es.json:
       - `Footer` namespace: quickNav labels, copyright, socialLabel
       - `Nav` namespace: add "about", "learn", "events", "contact" link labels
       - `Public` namespace: page-level SEO metadata keys (metaTitle, metaDescription for each page)
       - `Contact` namespace: page content keys
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Footer renders with 3-column layout, social icons, navigation links. WhatsApp bubble floats bottom-left with bilingual greeting. MobileNav provides hamburger slide-out with all navigation. Navbar enhanced with sticky behavior, public page links on desktop, hamburger on mobile. All i18n keys added for Footer, Nav, Public, and Contact namespaces.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npx tsc --noEmit` passes type checking
- Footer, WhatsApp bubble, motion wrappers, and MobileNav components exist with correct exports
- Navbar shows public page links and hamburger menu structure
- Content helper correctly typed and exports both functions
</verification>

<success_criteria>
- motion and openai packages in node_modules
- Migration 0006 ready for execution
- lib/content.ts exports getContentBlocks and getContentBlock
- 4 motion wrapper components in components/motion/ with "use client" directives
- Footer component renders nav links, social icons, WhatsApp, email
- WhatsApp bubble component with bilingual pre-filled greeting
- MobileNav with hamburger trigger and slide-out panel
- Navbar sticky with public page links (desktop) and hamburger (mobile)
- i18n keys added for Footer, Nav, Public, Contact namespaces in both languages
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-website-and-ai-chatbot/05-01-SUMMARY.md`
</output>
