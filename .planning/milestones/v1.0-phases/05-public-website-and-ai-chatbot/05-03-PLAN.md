---
phase: 05-public-website-and-ai-chatbot
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - app/api/chat/route.ts
  - components/chatbot/ChatWidget.tsx
  - components/chatbot/ChatBubble.tsx
  - components/chatbot/ChatPanel.tsx
  - app/[locale]/(marketing)/layout.tsx
  - messages/en.json
  - messages/es.json
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03
must_haves:
  truths:
    - "Visitor sees a floating chat bubble (bottom-right) on public pages"
    - "Clicking the bubble opens a chat panel with Nelly greeting and quick-reply chips"
    - "Visitor types a question in Spanish and receives a streaming Spanish response"
    - "Visitor types a question in English and receives a streaming English response"
    - "Nelly answers questions about pickleball rules, memberships, reservations, events, and locations using CMS content"
    - "Nelly politely declines off-topic questions"
    - "Chat history persists during session, resets on tab close"
    - "Rate limit of ~20 messages per session shows friendly redirect message"
  artifacts:
    - path: "app/api/chat/route.ts"
      provides: "Streaming Route Handler for AI chat with OpenAI"
      exports: ["POST"]
    - path: "components/chatbot/ChatWidget.tsx"
      provides: "Orchestrator: bubble + panel state, auto-greeting tooltip"
    - path: "components/chatbot/ChatBubble.tsx"
      provides: "Floating chat trigger button with pickleball icon"
    - path: "components/chatbot/ChatPanel.tsx"
      provides: "Chat window with messages, input, streaming display"
  key_links:
    - from: "components/chatbot/ChatPanel.tsx"
      to: "app/api/chat/route.ts"
      via: "fetch('/api/chat') with SSE stream reader"
      pattern: "fetch.*api/chat"
    - from: "app/api/chat/route.ts"
      to: "supabase content_blocks"
      via: "supabaseAdmin fetches all content_blocks for system prompt"
      pattern: "supabaseAdmin.*content_blocks"
    - from: "app/api/chat/route.ts"
      to: "openai"
      via: "openai.chat.completions.create with stream: true"
      pattern: "stream.*true"
    - from: "app/[locale]/(marketing)/layout.tsx"
      to: "components/chatbot/ChatWidget.tsx"
      via: "renders ChatWidget on all marketing pages"
      pattern: "ChatWidget"
user_setup:
  - service: openai
    why: "AI chatbot responses via gpt-4o-mini"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard (https://platform.openai.com/api-keys) -> Create new secret key"
---

<objective>
Build the "Nelly" AI chatbot: a streaming Route Handler powered by OpenAI gpt-4o-mini with prompt-stuffing from content_blocks, and a floating chat widget with bilingual support, quick-reply chips, typing indicator, and session-based rate limiting.

Purpose: Nelly provides instant bilingual support to visitors, answering questions about pickleball, memberships, reservations, events, and locations using the club's own CMS content as the knowledge base.
Output: Working AI chatbot embedded on all public pages with streaming responses.
</objective>

<execution_context>
@/Users/joseurizar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseurizar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-public-website-and-ai-chatbot/05-CONTEXT.md
@.planning/phases/05-public-website-and-ai-chatbot/05-RESEARCH.md
@.planning/phases/05-public-website-and-ai-chatbot/05-01-SUMMARY.md
@.planning/phases/05-public-website-and-ai-chatbot/05-02-SUMMARY.md

@lib/supabase/admin.ts
@lib/types/admin.ts

<interfaces>
<!-- From Plan 01 -->

From lib/supabase/admin.ts:
```typescript
export { supabaseAdmin }
// Service role client -- bypasses RLS. Used here because chat route is server-only
// and we want to fetch ALL content_blocks regardless of user auth state.
```

From lib/types/admin.ts:
```typescript
export interface ContentBlock {
  id: string; block_key: string; block_type: string;
  content_es: string | null; content_en: string | null;
  sort_order: number; updated_at: string;
}
export type EventType = 'tournament' | 'training' | 'social'
export interface Event { /* ... */ }
```

OpenAI SDK (installed in Plan 01):
```typescript
import OpenAI from 'openai'
const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY! })
// stream: true returns async iterable
```

<!-- From Plan 02 (must exist before this plan runs) -->

From app/[locale]/(marketing)/layout.tsx (created by Plan 02):
```typescript
// Server Component layout wrapping marketing pages
// Already imports: MotionProvider, Footer, WhatsAppBubble
// This plan ADDS ChatWidget import to this existing file
```

Brand colors: lime (#39FF14), midnight (#0B1D3A), turquoise (#1ED6C3), sunset (#FF6B2C)

Chatbot design decisions (from CONTEXT.md):
- Light chat panel: white/light background
- Nelly messages: gray bubbles
- User messages: lime bubbles with dark text
- Dark header bar with "Nelly" name, pickleball icon, close button
- Desktop: ~380px wide, ~520px tall, bottom-right
- Mobile: full-screen overlay
- Quick-reply chips: outlined pills with lime/turquoise border
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Streaming AI Route Handler with prompt-stuffing and rate limiting</name>
  <files>
    app/api/chat/route.ts
  </files>
  <action>
    Create `app/api/chat/route.ts` with a POST handler:

    1. Request body: `{ messages: Array<{ role: 'user' | 'assistant'; content: string }>, locale: string, sessionId: string }`

    2. Rate limiting (simple in-memory Map):
       - Track message count per sessionId in a Map with TTL
       - Limit: 20 messages per session
       - On limit hit: return JSON response `{ error: 'rate_limited', message: locale-appropriate friendly message }` with 429 status
       - Message: "I've answered a lot of questions! Visit our Contact page or WhatsApp for more help." / Spanish equivalent
       - Clean up stale sessions periodically (simple: delete entries older than 1 hour)

    3. Fetch content for system prompt:
       - Use `supabaseAdmin` (service role) to fetch ALL content_blocks: `supabaseAdmin.from('content_blocks').select('block_key, content_es, content_en')`
       - Also fetch upcoming events: `supabaseAdmin.from('events').select('title_es, title_en, description_es, description_en, event_date, event_type, start_time, end_time').gte('event_date', today).order('event_date').limit(10)`
       - Build knowledge string: map blocks to `[block_key]: content` format
       - Append events: format as readable event list

    4. System prompt (bilingual instruction):
       ```
       You are Nelly, the friendly AI assistant for NELL Pickleball Club in Bavaro, Dominican Republic.
       Your name plays on the club name NELL, inspired by founder Maria Nelly Mercedes Carrasco.

       RULES:
       - Detect the language of the user's message and ALWAYS respond in that same language
       - You help visitors with: pickleball rules, membership plans ($50/mo VIP all locations, $35/mo Basic one location), court reservations, upcoming events, and club locations
       - Be warm, enthusiastic, and encouraging about pickleball
       - Keep responses concise (2-3 paragraphs max)
       - If asked about something outside club topics, politely redirect: "I'm here to help with NELL Pickleball Club! For other questions, feel free to reach out via WhatsApp."
       - Never make up information not in the knowledge base
       - For membership signup, direct users to the Pricing page
       - For reservations, explain they need to create an account first

       CLUB KNOWLEDGE BASE:
       {knowledge}

       UPCOMING EVENTS:
       {events}
       ```

    5. OpenAI streaming call:
       - Model: `gpt-4o-mini`
       - `stream: true`
       - `max_tokens: 500`
       - `temperature: 0.7`
       - Messages: [system prompt, ...user messages (last 10 only to stay within context)]

    6. Convert OpenAI async iterable to ReadableStream SSE:
       - Each chunk: `data: {"text": "..."}\n\n`
       - On completion: `data: [DONE]\n\n`
       - Set headers: Content-Type text/event-stream, Cache-Control no-cache, Connection keep-alive

    7. Error handling:
       - If OPENAI_API_KEY not set: return 500 with error message
       - If OpenAI API errors: return 500 with generic "Nelly is unavailable right now"
       - Wrap stream in try/catch for mid-stream errors
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && test -f app/api/chat/route.ts && echo "Route exists"</automated>
  </verify>
  <done>Chat Route Handler at /api/chat accepts POST with messages array, fetches content_blocks + events for system prompt, calls OpenAI gpt-4o-mini with streaming, returns SSE ReadableStream. Rate limiting enforced at 20 messages per session. System prompt includes Nelly persona, bilingual instructions, and full club knowledge base.</done>
</task>

<task type="auto">
  <name>Task 2: Chat widget components (bubble, panel, orchestrator) and layout integration</name>
  <files>
    components/chatbot/ChatBubble.tsx,
    components/chatbot/ChatPanel.tsx,
    components/chatbot/ChatWidget.tsx,
    app/[locale]/(marketing)/layout.tsx,
    messages/en.json,
    messages/es.json
  </files>
  <action>
    1. Create `components/chatbot/ChatBubble.tsx` ("use client"):
       - Floating button: fixed bottom-right (right-6 bottom-6)
       - z-index: 50 (above WhatsApp bubble at z-40)
       - Circular button (56px) with pickleball/paddle SVG icon
       - Background: lime color, hover: scale up slightly
       - Props: `onClick`, `isOpen` (to toggle icon between chat and X)
       - When chat is open: show X close icon instead of chat icon
       - Subtle bounce animation on mount

    2. Create `components/chatbot/ChatPanel.tsx` ("use client"):
       - Props: `locale`, `onClose`
       - Desktop: positioned above bubble, ~380px wide, ~520px tall, bottom-right anchored
       - Mobile: full-screen overlay (fixed inset-0) with z-50
       - Detect mobile via media query or window.innerWidth

       Header bar:
       - Dark background (bg-midnight)
       - Pickleball icon avatar (small circle)
       - "Nelly" name text
       - Close button (X) at top-right

       Message area:
       - Light background (bg-white or bg-gray-50)
       - Scrollable message list, auto-scroll to bottom on new messages
       - Nelly messages: gray bubbles (bg-gray-200), left-aligned, small Nelly avatar
       - User messages: lime bubbles (bg-lime text-midnight), right-aligned
       - Typing indicator: three animated dots shown before stream starts, hidden on first token
       - Streaming text: append to current assistant message as chunks arrive

       Initial state:
       - Greeting message from Nelly: "Hola! Soy Nelly, tu asistente de NELL Pickleball Club. Como puedo ayudarte?" (or English based on locale)
       - Quick-reply chips below greeting (disappear after first user message):
         - "Membership plans" / "Planes de membresia"
         - "Court locations" / "Ubicaciones de canchas"
         - "How to play pickleball" / "Como jugar pickleball"
         - "Upcoming events" / "Proximos eventos"
       - Chips: outlined pill style with lime/turquoise border on light background

       Input area:
       - Text input at bottom with placeholder "Ask Nelly..." / "Pregunta a Nelly..."
       - Send button (arrow icon) -- lime accent
       - Disabled while streaming
       - Submit on Enter key (not Shift+Enter which adds newline)

       Message sending:
       - Generate sessionId on component mount (crypto.randomUUID or Math.random fallback)
       - On send: add user message to history, show typing indicator
       - Fetch POST `/api/chat` with messages array, locale, sessionId
       - Read response stream via `response.body.getReader()` + TextDecoder
       - Parse SSE lines: extract `data: {"text": "..."}` chunks
       - Append streamed text to current assistant message in real-time
       - On `[DONE]`: finalize message, hide typing indicator
       - On 429 (rate limit): show the returned error message as Nelly's response
       - History: useState array of messages, persists during component lifecycle (resets on unmount/tab close)
       - Limit messages sent to API to last 10 (keep full history for display)

    3. Create `components/chatbot/ChatWidget.tsx` ("use client"):
       - Orchestrates ChatBubble + ChatPanel
       - State: `isOpen` (boolean)
       - Props: `locale`
       - Renders ChatBubble always
       - Renders ChatPanel conditionally when isOpen (with AnimatePresence for enter/exit animation)
       - Auto-greeting tooltip: after 5-8 seconds on page, show small tooltip near bubble:
         "Tienes preguntas? Pregunta a Nelly!" / "Have questions? Ask Nelly!"
         - Tooltip disappears after 5 seconds or on bubble click
         - Only shows once per session (sessionStorage flag)

    4. Update `app/[locale]/(marketing)/layout.tsx` (MODIFIES existing file created by Plan 02):
       - Import ChatWidget from `@/components/chatbot/ChatWidget`
       - Render `<ChatWidget locale={locale} />` in the layout alongside existing MotionProvider, Footer, and WhatsAppBubble
       - ChatWidget and WhatsAppBubble should both be present (opposite corners: WhatsApp bottom-left, Nelly bottom-right)

    5. Add i18n keys to messages/en.json and messages/es.json:
       - `Chatbot` namespace: greeting, quickReply chips, inputPlaceholder, tooltipText, rateLimitMessage, unavailableMessage
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && test -f components/chatbot/ChatWidget.tsx && test -f components/chatbot/ChatPanel.tsx && test -f components/chatbot/ChatBubble.tsx && echo "All chatbot components exist"</automated>
  </verify>
  <done>Chat bubble floats bottom-right on all public pages. Clicking opens chat panel with Nelly greeting and 4 quick-reply chips. User can type messages and receive streaming responses word-by-word. Typing indicator shown before stream starts. Mobile: full-screen overlay. Desktop: 380x520 panel. Auto-greeting tooltip after 5-8 seconds. Rate limit at 20 messages shows friendly redirect. Chat history persists during session.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Chat route handler exists at app/api/chat/route.ts
- ChatWidget, ChatBubble, ChatPanel components exist in components/chatbot/
- Marketing layout includes both ChatWidget and WhatsAppBubble
- TypeScript compilation passes
</verification>

<success_criteria>
- Floating chat bubble visible bottom-right on all public pages
- Chat panel opens with Nelly greeting and quick-reply chips
- Quick-reply chips disappear after first user message
- Streaming responses appear word-by-word from OpenAI gpt-4o-mini
- Nelly responds in the same language the user writes in
- Nelly answers club-related questions using CMS content_blocks as knowledge base
- Nelly politely declines off-topic questions
- Typing indicator shown before stream starts
- Mobile: full-screen chat overlay
- Rate limit at ~20 messages shows friendly redirect to Contact/WhatsApp
- Auto-greeting tooltip appears once after 5-8 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-website-and-ai-chatbot/05-03-SUMMARY.md`
</output>
