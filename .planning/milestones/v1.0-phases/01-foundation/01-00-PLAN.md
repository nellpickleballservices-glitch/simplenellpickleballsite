---
phase: 01-foundation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - playwright.config.ts
  - tests/unit/normalizeName.test.ts
  - tests/unit/passwordValidation.test.ts
  - tests/unit/adminRole.test.ts
  - tests/unit/rls-policies.test.ts
  - tests/unit/proxyUsesGetUser.test.ts
  - tests/unit/noHardcodedStrings.test.ts
  - tests/auth/signup.spec.ts
  - tests/auth/login.spec.ts
  - tests/auth/session-persist.spec.ts
  - tests/auth/password-reset.spec.ts
  - tests/auth/route-protection.spec.ts
  - tests/i18n/locale-routing.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running `npx vitest run` exits 0 (all unit stubs pass or skip)"
    - "Running `npx playwright test --list` lists 6 e2e spec files without error"
    - "No implementation code is written — stubs are skeletons only"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest test runner configuration"
    - path: "playwright.config.ts"
      provides: "Playwright e2e test runner configuration"
    - path: "tests/unit/normalizeName.test.ts"
      provides: "Stub for AUTH-02 name normalization unit tests"
    - path: "tests/unit/passwordValidation.test.ts"
      provides: "Stub for AUTH-03 password validation unit tests"
    - path: "tests/unit/adminRole.test.ts"
      provides: "Stub for AUTH-07 admin role unit tests"
    - path: "tests/unit/rls-policies.test.ts"
      provides: "Stub for SEC-01, SEC-03 RLS policy unit tests"
    - path: "tests/unit/proxyUsesGetUser.test.ts"
      provides: "Stub for SEC-02 proxy source-check unit test"
    - path: "tests/unit/noHardcodedStrings.test.ts"
      provides: "Stub for I18N-02 hardcoded string AST check"
    - path: "tests/auth/signup.spec.ts"
      provides: "Stub for AUTH-01 e2e signup spec"
    - path: "tests/auth/login.spec.ts"
      provides: "Stub for AUTH-04 e2e login spec"
    - path: "tests/auth/session-persist.spec.ts"
      provides: "Stub for AUTH-05 e2e session persistence spec"
    - path: "tests/auth/password-reset.spec.ts"
      provides: "Stub for AUTH-06 e2e password reset spec"
    - path: "tests/auth/route-protection.spec.ts"
      provides: "Stub for SEC-05 e2e route protection spec"
    - path: "tests/i18n/locale-routing.spec.ts"
      provides: "Stub for I18N-01 e2e locale routing spec"
  key_links:
    - from: "vitest.config.ts"
      to: "tests/unit/*.test.ts"
      via: "include glob"
      pattern: "tests/unit"
    - from: "playwright.config.ts"
      to: "tests/auth/*.spec.ts, tests/i18n/*.spec.ts"
      via: "testDir"
      pattern: "testDir.*tests"
---

<objective>
Install test frameworks (Vitest + Playwright) and create all Wave 0 test stubs that subsequent plans will fill in. This is the Nyquist compliance gate — no implementation plan may begin until every automated verify command in Phase 1 resolves to an existing file.

Purpose: Ensures every `<automated>` command in plans 01-01 through 01-04 points to a real file from the moment it first runs (fails fast on RED, not on "file not found").
Output: vitest.config.ts, playwright.config.ts, 6 unit test stubs, 6 e2e test stubs (all skipped/todo — not passing).
</objective>

<execution_context>
@/Users/joseurizar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseurizar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joseurizar/desktop/nell_pickleball_club/.planning/PROJECT.md
@/Users/joseurizar/desktop/nell_pickleball_club/.planning/ROADMAP.md
@/Users/joseurizar/desktop/nell_pickleball_club/.planning/STATE.md
@/Users/joseurizar/desktop/nell_pickleball_club/.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test frameworks and write config files</name>
  <files>
    package.json (dev deps added),
    vitest.config.ts,
    playwright.config.ts
  </files>
  <action>
    Run from project root:
    ```
    npm install -D vitest @vitest/ui
    npm install -D @playwright/test
    npx playwright install --with-deps chromium
    ```

    Create `vitest.config.ts`:
    ```typescript
    import { defineConfig } from 'vitest/config'
    import path from 'path'

    export default defineConfig({
      test: {
        environment: 'node',
        include: ['tests/unit/**/*.test.ts'],
        globals: true,
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, '.'),
        },
      },
    })
    ```

    Create `playwright.config.ts`:
    ```typescript
    import { defineConfig, devices } from '@playwright/test'

    export default defineConfig({
      testDir: './tests',
      fullyParallel: true,
      forbidOnly: !!process.env.CI,
      retries: process.env.CI ? 2 : 0,
      workers: process.env.CI ? 1 : undefined,
      reporter: 'list',
      use: {
        baseURL: 'http://localhost:3000',
        trace: 'on-first-retry',
      },
      projects: [
        {
          name: 'chromium',
          use: { ...devices['Desktop Chrome'] },
        },
      ],
    })
    ```

    Do NOT add a `webServer` block — the dev server will be started manually during e2e runs.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | head -5 || true</automated>
  </verify>
  <done>
    Both config files exist. `npx vitest run` exits without "config not found" error (may exit with no tests found — that is fine at this stage).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create all unit test stubs</name>
  <files>
    tests/unit/normalizeName.test.ts,
    tests/unit/passwordValidation.test.ts,
    tests/unit/adminRole.test.ts,
    tests/unit/rls-policies.test.ts,
    tests/unit/proxyUsesGetUser.test.ts,
    tests/unit/noHardcodedStrings.test.ts
  </files>
  <action>
    Create `tests/unit/normalizeName.test.ts` (covers AUTH-02):
    ```typescript
    import { describe, it, expect } from 'vitest'

    // Stub: will import from lib/utils/normalizeName.ts once created in plan 01-03
    describe('normalizeName', () => {
      it.todo('trims leading and trailing whitespace')
      it.todo('capitalizes first letter of each word')
      it.todo('lowercases the rest of each word')
      it.todo('"jose urizar" → "Jose Urizar"')
      it.todo('rejects names containing numbers')
      it.todo('rejects empty string after trim')
      it.todo('handles accented characters: "maría" → "María"')
    })
    ```

    Create `tests/unit/passwordValidation.test.ts` (covers AUTH-03):
    ```typescript
    import { describe, it, expect } from 'vitest'

    // Stub: will import from lib/utils/passwordValidation.ts once created in plan 01-03
    describe('passwordValidation', () => {
      it.todo('rejects passwords shorter than 8 characters')
      it.todo('accepts passwords of exactly 8 characters')
      it.todo('accepts passwords longer than 8 characters')
      it.todo('validatePasswordMatch: returns null when passwords match')
      it.todo('validatePasswordMatch: returns error string when passwords do not match')
    })
    ```

    Create `tests/unit/adminRole.test.ts` (covers AUTH-07):
    ```typescript
    import { describe, it, expect } from 'vitest'

    // Stub: verifies admin role is written to app_metadata (not user_metadata)
    // Will use Supabase test client patterns once plan 01-02 creates the admin utility
    describe('adminRole', () => {
      it.todo('assignAdminRole writes to app_metadata.role, not user_metadata.role')
      it.todo('a user cannot self-assign admin role via signUp metadata')
      it.todo('admin check reads from user.app_metadata.role')
    })
    ```

    Create `tests/unit/rls-policies.test.ts` (covers SEC-01, SEC-03):
    ```typescript
    import { describe, it, expect } from 'vitest'

    // Stub: will use Supabase test client with anon key to verify RLS
    // Tests run against a real Supabase project (requires NEXT_PUBLIC_SUPABASE_URL etc.)
    describe('RLS policies', () => {
      describe('profiles table', () => {
        it.todo('RLS is enabled on profiles table')
        it.todo('user can read their own profile row')
        it.todo('user cannot read another user\'s profile row')
      })
      describe('memberships table', () => {
        it.todo('RLS is enabled on memberships table')
        it.todo('user can read their own membership row')
        it.todo('user cannot read another user\'s membership row')
      })
      describe('reservations table', () => {
        it.todo('RLS is enabled on reservations table')
        it.todo('user can read their own reservation rows')
        it.todo('user cannot read another user\'s reservation rows')
      })
    })
    ```

    Create `tests/unit/proxyUsesGetUser.test.ts` (covers SEC-02):
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'fs'
    import { join } from 'path'

    // Source-code check: verifies proxy.ts calls getUser() and not getSession()
    describe('proxy.ts security checks', () => {
      it('proxy.ts uses getUser() not getSession()', () => {
        // This test will pass once proxy.ts exists (plan 01-01)
        // Until then it is skipped
        const proxyPath = join(process.cwd(), 'proxy.ts')
        let source: string
        try {
          source = readFileSync(proxyPath, 'utf-8')
        } catch {
          // File does not exist yet — skip until plan 01-01 creates it
          return
        }
        expect(source).toContain('getUser()')
        expect(source).not.toContain('getSession()')
      })
    })
    ```

    Create `tests/unit/noHardcodedStrings.test.ts` (covers I18N-02):
    ```typescript
    import { describe, it, expect } from 'vitest'

    // Stub: AST / grep check that no component files contain hardcoded Spanish/English UI strings
    // Implementation will scan app/**/*.tsx for string literals not behind t()
    describe('No hardcoded UI strings', () => {
      it.todo('app/[locale]/layout.tsx contains no hardcoded UI strings')
      it.todo('auth pages contain no hardcoded UI strings')
      it.todo('signup page renders all text via useTranslations()')
    })
    ```
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    All 6 unit stub files exist in tests/unit/. `npx vitest run` exits 0. All tests show as `todo` or `skipped` — none are failing red.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create all e2e test stubs</name>
  <files>
    tests/auth/signup.spec.ts,
    tests/auth/login.spec.ts,
    tests/auth/session-persist.spec.ts,
    tests/auth/password-reset.spec.ts,
    tests/auth/route-protection.spec.ts,
    tests/i18n/locale-routing.spec.ts
  </files>
  <action>
    Create `tests/auth/signup.spec.ts` (covers AUTH-01):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('Signup flow (AUTH-01)', () => {
      test.todo('signup page renders all required fields: first name, last name, email, phone, password, confirm password')
      test.todo('plan selection shows VIP ($50/mo) and Basic ($35/mo) cards side by side')
      test.todo('plan selection is optional — user can submit without selecting a plan')
      test.todo('successful signup redirects to home page with user signed in')
      test.todo('welcome banner appears with user name and fades after ~3 seconds')
      test.todo('Google OAuth button is present on signup page')
    })
    ```

    Create `tests/auth/login.spec.ts` (covers AUTH-04):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('Login flow (AUTH-04)', () => {
      test.todo('login page renders email/password form')
      test.todo('login page shows "Sign in with Google" button')
      test.todo('login page shows "Forgot password?" link')
      test.todo('valid credentials redirect to home page')
      test.todo('invalid credentials show inline error message')
    })
    ```

    Create `tests/auth/session-persist.spec.ts` (covers AUTH-05):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('Session persistence (AUTH-05)', () => {
      test.todo('authenticated user does not need to re-login after page reload')
      test.todo('session cookie is set as httpOnly after successful login')
    })
    ```

    Create `tests/auth/password-reset.spec.ts` (covers AUTH-06):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('Password reset flow (AUTH-06)', () => {
      test.todo('reset-password page renders email input and submit button')
      test.todo('submitting a valid email shows confirmation message')
      test.todo('reset-password/update page renders new password and confirm password fields')
      // Note: full email delivery cannot be tested in CI — manual verification required
    })
    ```

    Create `tests/auth/route-protection.spec.ts` (covers SEC-05):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('Route protection (SEC-05)', () => {
      test.todo('unauthenticated GET /dashboard redirects to /login')
      test.todo('unauthenticated GET /en/dashboard redirects to /en/login')
      test.todo('unauthenticated GET /admin redirects to /login')
      test.todo('authenticated non-admin GET /admin is rejected (403 or redirect)')
    })
    ```

    Create `tests/i18n/locale-routing.spec.ts` (covers I18N-01):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('Locale routing (I18N-01)', () => {
      test.todo('GET / responds 200 and renders Spanish content (no locale prefix)')
      test.todo('GET /en/ responds 200 and renders English content')
      test.todo('GET /es/ redirects to / (default locale has no prefix)')
      test.todo('language switcher in nav changes locale and redirects to same page in other language')
      test.todo('language preference cookie is set when switching language as guest')
    })
    ```
  </action>
  <verify>
    <automated>npx playwright test --list 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 6 e2e stub files exist. `npx playwright test --list` lists 0 runnable tests (all are `test.todo`) without errors. `npx vitest run` still exits 0 with all unit stubs passing/todo.
  </done>
</task>

</tasks>

<verification>
Full Wave 0 gate:
```
npx vitest run --reporter=verbose
npx playwright test --list
```

Both commands must exit 0. Vitest shows all tests as `todo`. Playwright lists files without errors.
</verification>

<success_criteria>
- vitest.config.ts and playwright.config.ts exist and are valid
- All 12 test stub files exist in tests/unit/ and tests/auth/ and tests/i18n/
- `npx vitest run` exits 0
- `npx playwright test --list` exits 0
- Zero implementation code has been written
- 01-VALIDATION.md Wave 0 checklist items can all be ticked off
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md`
</output>
