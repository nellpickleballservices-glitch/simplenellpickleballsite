---
phase: 04-admin-and-cms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0005_admin_events_cms.sql
  - proxy.ts
  - app/[locale]/(admin)/admin/layout.tsx
  - app/[locale]/(admin)/admin/page.tsx
  - components/admin/AdminSidebar.tsx
  - components/admin/StatCard.tsx
  - components/admin/ConfirmDialog.tsx
  - components/Navbar.tsx
  - app/actions/admin.ts
  - lib/types/admin.ts
  - messages/en.json
  - messages/es.json
autonomous: true
requirements:
  - ADMIN-11
  - ADMIN-09
  - CMS-01

must_haves:
  truths:
    - "Non-admin user accessing /admin/* is redirected at proxy middleware level"
    - "Non-admin user passing proxy is rejected at layout level"
    - "Admin user sees sidebar navigation with all admin sections"
    - "Admin dashboard shows stat cards with total users, active members, today reservations, upcoming events"
    - "Admin link appears in Navbar only for admin-role users"
  artifacts:
    - path: "supabase/migrations/0005_admin_events_cms.sql"
      provides: "Events columns, maintenance columns, content_blocks seed data, created_by_admin column"
      contains: "ALTER TABLE events"
    - path: "proxy.ts"
      provides: "Layer 1 admin route protection"
      contains: "app_metadata"
    - path: "app/[locale]/(admin)/admin/layout.tsx"
      provides: "Layer 2 admin role gate + sidebar shell"
      contains: "role !== 'admin'"
    - path: "app/actions/admin.ts"
      provides: "requireAdmin helper + admin Server Actions"
      exports: ["requireAdmin", "getAdminStatsAction"]
    - path: "lib/types/admin.ts"
      provides: "TypeScript types for admin domain"
    - path: "components/admin/AdminSidebar.tsx"
      provides: "Fixed left sidebar with icon+label nav links"
    - path: "components/admin/ConfirmDialog.tsx"
      provides: "Reusable confirmation dialog for destructive actions"
  key_links:
    - from: "proxy.ts"
      to: "supabase.auth.getUser()"
      via: "app_metadata.role check for /admin/* paths"
      pattern: "app_metadata.*role.*admin"
    - from: "app/[locale]/(admin)/admin/layout.tsx"
      to: "supabase.auth.getUser()"
      via: "server-side role check before rendering"
      pattern: "app_metadata.*role.*admin"
    - from: "app/actions/admin.ts"
      to: "lib/supabase/admin.ts"
      via: "supabaseAdmin for service-role operations"
      pattern: "supabaseAdmin"
---

<objective>
Admin foundation: database migration (events columns, maintenance columns, content_blocks seed data), three-layer admin route protection, admin layout with sidebar, dashboard stats page, shared admin components, and admin type definitions.

Purpose: Every subsequent admin plan depends on the route protection, layout shell, requireAdmin helper, and migration being in place. This plan establishes the admin foundation.
Output: Working admin dashboard at /admin with sidebar, stat cards, and complete route protection at all three layers.
</objective>

<execution_context>
@/Users/joseurizar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseurizar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-and-cms/04-CONTEXT.md
@.planning/phases/04-admin-and-cms/04-RESEARCH.md

@proxy.ts
@lib/supabase/admin.ts
@lib/supabase/server.ts
@components/Navbar.tsx
@app/[locale]/(admin)/admin/page.tsx
@supabase/migrations/0001_initial_schema.sql
@supabase/migrations/0003_reservations.sql
@lib/types/reservations.ts
@messages/en.json
@messages/es.json

<interfaces>
<!-- Existing types and patterns the executor needs -->

From lib/supabase/admin.ts:
```typescript
export const supabaseAdmin: SupabaseClient  // service-role, bypasses RLS
export async function assignAdminRole(userId: string): Promise<void>
```

From lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From lib/types/reservations.ts:
```typescript
export type CourtStatus = 'open' | 'closed' | 'maintenance'
export interface Court { id: string; location_id: string; name: string; status: CourtStatus; ... }
export interface Location { id: string; name: string; address: string | null; lat: number | null; lng: number | null; ... }
```

Existing events table columns (from 0001_initial_schema.sql):
```sql
events: id, title_es, title_en, description_es, description_en, event_date, location_id, created_at
```

Existing content_blocks table columns (from 0001_initial_schema.sql):
```sql
content_blocks: id, block_key (UNIQUE), block_type, content_es, content_en, sort_order, updated_at
```

Brand colors (from globals.css):
- Electric Lime: #39FF14 (--color-lime)
- Midnight Blue: #0B1D3A (--color-midnight)
- Caribbean Turquoise: #1ED6C3 (--color-turquoise)
- Sunset Orange: #FF6B2C (--color-sunset)
- Off-white: #F7F9FC (--color-offwhite)
- Charcoal: #2A2A2A (--color-charcoal)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration, types, and requireAdmin helper</name>
  <files>
    supabase/migrations/0005_admin_events_cms.sql,
    lib/types/admin.ts,
    app/actions/admin.ts
  </files>
  <action>
1. Create `supabase/migrations/0005_admin_events_cms.sql`:
   - ALTER TABLE events ADD COLUMN event_type TEXT NOT NULL DEFAULT 'social' CHECK (event_type IN ('tournament', 'training', 'social'))
   - ALTER TABLE events ADD COLUMN start_time TIME, ADD COLUMN end_time TIME, ADD COLUMN image_url TEXT
   - ALTER TABLE courts ADD COLUMN maintenance_start TIMESTAMPTZ, ADD COLUMN maintenance_end TIMESTAMPTZ
   - ALTER TABLE reservations ADD COLUMN created_by_admin BOOLEAN DEFAULT FALSE
   - Seed content_blocks with all Phase 5 page blocks (ON CONFLICT DO NOTHING):
     - home_hero (rich_text, sort_order 1), home_overview (rich_text, sort_order 2)
     - about_vision (rich_text, 1), about_mission (rich_text, 2), about_values (rich_text, 3)
     - learn_origin (rich_text, 1), learn_rules (rich_text, 2), learn_scoring (rich_text, 3), learn_equipment (rich_text, 4)
     - faq_general (rich_text, 1), faq_membership (rich_text, 2), faq_reservations (rich_text, 3), faq_rules (rich_text, 4)
   - Each block gets placeholder content_es and content_en (e.g., '<h2>Bienvenido a NELL</h2><p>Contenido pendiente...</p>')

2. Create `lib/types/admin.ts`:
   - Export EventType = 'tournament' | 'training' | 'social'
   - Export Event interface (all columns including new ones)
   - Export ContentBlock interface (id, block_key, block_type, content_es, content_en, sort_order, updated_at)
   - Export AdminStats interface { totalUsers: number; activeMembers: number; todayReservations: number; upcomingEvents: number }
   - Export UserWithDetails interface { id, email, first_name, last_name, phone, created_at, membership_status, membership_plan, is_banned }

3. Create `app/actions/admin.ts`:
   - 'use server' directive
   - Import createClient from @/lib/supabase/server and supabaseAdmin from @/lib/supabase/admin
   - requireAdmin() helper: calls createClient(), getUser(), checks user.app_metadata?.role === 'admin', redirects to '/' if not admin. Returns user. This is Layer 3 protection.
   - getAdminStatsAction(): calls requireAdmin(), then queries via supabaseAdmin:
     - Total users: supabaseAdmin.auth.admin.listUsers({ page: 1, perPage: 1 }) to get total (or count profiles)
     - Active members: supabaseAdmin.from('memberships').select('id', { count: 'exact' }).eq('status', 'active')
     - Today's reservations: supabaseAdmin.from('reservations').select('id', { count: 'exact' }).gte('starts_at', todayStart).lt('starts_at', tomorrowStart).neq('status', 'cancelled')
     - Upcoming events: supabaseAdmin.from('events').select('id', { count: 'exact' }).gte('event_date', now)
   - Use America/Santo_Domingo timezone for today calculations (consistent with Phase 3)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Migration file ready for SQL Editor. Admin types exported. requireAdmin helper and getAdminStatsAction working with TypeScript compilation passing.</done>
</task>

<task type="auto">
  <name>Task 2: Three-layer route protection, admin layout with sidebar, dashboard page, and Navbar admin link</name>
  <files>
    proxy.ts,
    app/[locale]/(admin)/admin/layout.tsx,
    app/[locale]/(admin)/admin/page.tsx,
    components/admin/AdminSidebar.tsx,
    components/admin/StatCard.tsx,
    components/admin/ConfirmDialog.tsx,
    components/Navbar.tsx,
    messages/en.json,
    messages/es.json
  </files>
  <action>
1. Update `proxy.ts` (Layer 1 admin protection):
   - After the existing unauthenticated redirect block and BEFORE the membership gate block, add:
   - If user exists AND pathname.includes('/admin'), check user.app_metadata?.role === 'admin'. If not admin, redirect to '/'.
   - Keep all existing reservation route exceptions and membership gating untouched.

2. Create `app/[locale]/(admin)/admin/layout.tsx` (Layer 2):
   - Server Component. Import createClient, call getUser().
   - If !user, redirect('/login'). If user.app_metadata?.role !== 'admin', redirect('/').
   - Render flex layout: AdminSidebar on left + main content area with bg-[#0a1628] (slightly lighter than midnight for contrast), p-6, overflow-y-auto.
   - Pass locale to AdminSidebar for i18n.

3. Create `components/admin/AdminSidebar.tsx`:
   - 'use client' component (needs useState for mobile toggle).
   - Fixed left sidebar, w-64 on desktop, bg-midnight (#0B1D3A), text-offwhite.
   - Top: NELL brand text (font-bebas-neue, text-lime).
   - Nav links with icon + label: Dashboard (/admin), Users (/admin/users), Courts (/admin/courts), Reservations (/admin/reservations), Events (/admin/events), CMS (/admin/cms), Stripe (/admin/stripe).
   - Use simple SVG icons or Unicode symbols for each nav item (no icon library needed).
   - Active state: Electric Lime left border-l-4 + text-lime on active link. Use usePathname() from next/navigation to detect active.
   - Mobile: hamburger button visible on md:hidden, sidebar slides in as overlay with backdrop. Close on link click or backdrop click.
   - Use useTranslations('Admin') for all labels.

4. Create `components/admin/StatCard.tsx`:
   - Props: { title: string; value: number; icon?: string }
   - Card with bg-[#111b2e] (dark panel), rounded-lg, p-6. Title in text-gray-400 text-sm, value in text-3xl font-bold text-offwhite. Optional icon in top-right corner.

5. Create `components/admin/ConfirmDialog.tsx`:
   - 'use client' component. Props: { open: boolean; onClose: () => void; onConfirm: () => void; title: string; message: string; confirmLabel?: string; destructive?: boolean; loading?: boolean }
   - Modal overlay (bg-black/50 fixed inset-0). Centered panel with bg-[#111b2e], rounded-lg, p-6.
   - Title, message text, and two buttons: Cancel (ghost style) + Confirm (bg-red-600 if destructive, bg-lime if not). Confirm button shows loading spinner when loading=true.
   - Close on Escape key and backdrop click.

6. Replace `app/[locale]/(admin)/admin/page.tsx`:
   - Server Component. Import getAdminStatsAction and useTranslations.
   - Call getAdminStatsAction() to get stats.
   - Render h1 "Admin Dashboard", then 4 StatCard components in a responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6).
   - Cards: Total Users, Active Members, Today's Reservations, Upcoming Events.

7. Update `components/Navbar.tsx`:
   - After getting the user, check user.app_metadata?.role === 'admin'.
   - If admin, render an "Admin" link to /admin between the dashboard link and logout button. Style same as other nav links with text-offwhite hover:text-lime.

8. Update `messages/en.json` and `messages/es.json`:
   - Add "Admin" namespace with keys: dashboard, users, courts, reservations, events, cms, stripe, totalUsers, activeMembers, todayReservations, upcomingEvents, confirmDelete, confirmDisable, cancel, confirm, adminNav (for Navbar link).
   - Add admin-specific keys for stat card titles, sidebar labels.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Non-admin users are redirected at proxy.ts (Layer 1) and layout.tsx (Layer 2). Admin sees sidebar with all nav links, dashboard with 4 stat cards. Navbar shows "Admin" link only for admin-role users. ConfirmDialog component ready for all subsequent plans.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- proxy.ts contains admin role check for /admin/* paths
- layout.tsx contains independent admin role check
- AdminSidebar renders 7 navigation links
- Dashboard page renders 4 stat cards
- Navbar conditionally shows Admin link
- Migration file contains events columns, maintenance columns, content_blocks seed data
</verification>

<success_criteria>
- Three-layer route protection in place (proxy, layout, requireAdmin in actions)
- Admin dashboard renders at /admin with stat cards
- Sidebar navigation links to all 7 admin sections
- Admin link visible in main Navbar only for admin users
- Migration 0005 ready with events enhancements + content_blocks seeds
- ConfirmDialog reusable component available for all admin plans
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-and-cms/04-01-SUMMARY.md`
</output>
