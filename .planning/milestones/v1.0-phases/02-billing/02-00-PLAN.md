---
phase: 02-billing
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/billing.test.ts
  - tests/unit/webhookHandler.test.ts
  - tests/unit/proxyMembership.test.ts
  - tests/unit/checkoutSuccess.test.ts
  - supabase/migrations/0002_webhook_events.sql
autonomous: true
requirements: [BILL-01, BILL-02, BILL-03, BILL-04, BILL-05, BILL-06, BILL-07, BILL-08, BILL-09]

must_haves:
  truths:
    - "Every BILL requirement has a corresponding test stub ready for RED-GREEN execution"
    - "webhook_events table schema exists with UNIQUE on stripe_event_id"
    - "memberships table has UNIQUE constraint on user_id for upsert support"
    - "memberships table is added to supabase_realtime publication"
  artifacts:
    - path: "tests/unit/billing.test.ts"
      provides: "Test stubs for checkout session creation and portal session creation"
      contains: "test.skip"
    - path: "tests/unit/webhookHandler.test.ts"
      provides: "Test stubs for all 5 webhook event types, idempotency, signature verification"
      contains: "test.skip"
    - path: "tests/unit/proxyMembership.test.ts"
      provides: "Test stubs for proxy membership gating (active passes, cancelled/past_due redirects)"
      contains: "test.skip"
    - path: "tests/unit/checkoutSuccess.test.ts"
      provides: "Test stubs for Realtime component state transitions"
      contains: "test.skip"
    - path: "supabase/migrations/0002_webhook_events.sql"
      provides: "webhook_events table, UNIQUE(user_id) on memberships, Realtime publication"
      contains: "webhook_events"
  key_links: []
---

<objective>
Wave 0 Nyquist gate: create all test stubs for Phase 2 billing requirements and the database migration that adds the webhook_events idempotency table, UNIQUE constraint on memberships.user_id (required for upsert), and adds memberships to the supabase_realtime publication (required for post-checkout Realtime listener).

Purpose: Every subsequent plan has test files ready for RED-GREEN execution. Migration must be run in Supabase Dashboard SQL Editor before plans 02-01 through 02-03 execute.
Output: 4 test stub files + 1 SQL migration file.
</objective>

<execution_context>
@/Users/joseurizar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joseurizar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-billing/02-RESEARCH.md
@.planning/phases/02-billing/02-CONTEXT.md

@tests/unit/normalizeName.test.ts (existing test pattern reference)
@supabase/migrations/0001_initial_schema.sql (existing schema — memberships table definition)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all Phase 2 unit test stubs</name>
  <files>tests/unit/billing.test.ts, tests/unit/webhookHandler.test.ts, tests/unit/proxyMembership.test.ts, tests/unit/checkoutSuccess.test.ts</files>
  <action>
Create 4 test stub files following the existing test pattern from tests/unit/normalizeName.test.ts. Use `test.skip()` for each test case (test.todo() is not available in Vitest — see Phase 1 decision).

**tests/unit/billing.test.ts** — covers BILL-01, BILL-02, BILL-07:
- test.skip: "createCheckoutSessionAction passes correct price ID for VIP plan"
- test.skip: "createCheckoutSessionAction passes correct price ID for Basic plan"
- test.skip: "createCheckoutSessionAction sets client_reference_id to supabase user UUID"
- test.skip: "createCheckoutSessionAction sets mode to subscription"
- test.skip: "createCheckoutSessionAction redirects to Stripe checkout URL"
- test.skip: "createPortalSessionAction fetches stripe_customer_id from memberships"
- test.skip: "createPortalSessionAction creates portal session with correct customer ID"
- test.skip: "createPortalSessionAction redirects to portal URL"

**tests/unit/webhookHandler.test.ts** — covers BILL-03, BILL-04, BILL-05, BILL-06, BILL-08:
- test.skip: "returns 400 when Stripe signature is invalid" (BILL-05)
- test.skip: "returns 200 and skips processing for duplicate stripe_event_id" (BILL-04)
- test.skip: "checkout.session.completed upserts memberships row with active status" (BILL-03, BILL-06)
- test.skip: "checkout.session.completed extracts client_reference_id as user_id" (BILL-02)
- test.skip: "checkout.session.completed sets plan_type from price ID" (BILL-01)
- test.skip: "customer.subscription.updated changes plan_type on upgrade" (BILL-07)
- test.skip: "customer.subscription.updated sets status to past_due on payment failure" (BILL-03)
- test.skip: "customer.subscription.deleted sets status to cancelled" (BILL-08)
- test.skip: "invoice.payment_succeeded updates current_period_end" (BILL-03)
- test.skip: "invoice.payment_failed sets status to past_due" (BILL-03)

**tests/unit/proxyMembership.test.ts** — covers BILL-09:
- test.skip: "active membership allows access to /member/ routes"
- test.skip: "cancelled membership redirects to /pricing"
- test.skip: "past_due membership redirects to /pricing"
- test.skip: "no membership row redirects to /pricing"

**tests/unit/checkoutSuccess.test.ts** — covers Success Criterion 1:
- test.skip: "renders pending state initially"
- test.skip: "transitions to active when Realtime fires UPDATE with status active"
- test.skip: "shows timeout message after 30 seconds with no update"

Each file should import from `vitest` (`describe`, `test`, `expect`) and have a top-level `describe` block grouping the tests.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/billing.test.ts tests/unit/webhookHandler.test.ts tests/unit/proxyMembership.test.ts tests/unit/checkoutSuccess.test.ts 2>&1 | grep -E "(skipped|Tests)"</automated>
  </verify>
  <done>All 4 test files exist, all tests are skipped (not failing), vitest reports 0 failures</done>
</task>

<task type="auto">
  <name>Task 2: Create migration 0002 — webhook_events table, memberships UNIQUE, Realtime publication</name>
  <files>supabase/migrations/0002_webhook_events.sql</files>
  <action>
Create `supabase/migrations/0002_webhook_events.sql` with three sections:

**Section 1: webhook_events table**
```sql
CREATE TABLE webhook_events (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  stripe_event_id TEXT NOT NULL UNIQUE,
  event_type      TEXT NOT NULL,
  processed_at    TIMESTAMPTZ DEFAULT NOW()
);
ALTER TABLE webhook_events ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Service role full access on webhook_events"
  ON webhook_events FOR ALL TO service_role USING (true) WITH CHECK (true);
```
No user-facing RLS policies needed — only service_role writes to this table from the webhook handler.

**Section 2: UNIQUE constraint on memberships.user_id**
```sql
ALTER TABLE memberships ADD CONSTRAINT memberships_user_id_key UNIQUE (user_id);
```
This is required for the Supabase `upsert({ onConflict: 'user_id' })` pattern in the webhook handler. One user = one membership row.

**Section 3: Add memberships to supabase_realtime publication**
```sql
ALTER PUBLICATION supabase_realtime ADD TABLE memberships;
```
Required for the post-checkout Realtime listener (Pitfall 3 from RESEARCH.md — without this, Realtime subscription reports SUBSCRIBED but never fires change events).

Add a header comment: "Run in: Supabase Dashboard -> SQL Editor (after 0001_initial_schema.sql)"
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE webhook_events" supabase/migrations/0002_webhook_events.sql && grep -c "UNIQUE" supabase/migrations/0002_webhook_events.sql && grep -c "supabase_realtime" supabase/migrations/0002_webhook_events.sql</automated>
  </verify>
  <done>Migration file exists with webhook_events table (UNIQUE on stripe_event_id), memberships UNIQUE(user_id) constraint, and supabase_realtime publication including memberships</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/billing.test.ts tests/unit/webhookHandler.test.ts tests/unit/proxyMembership.test.ts tests/unit/checkoutSuccess.test.ts` — all tests skipped, 0 failures
- `cat supabase/migrations/0002_webhook_events.sql` — contains webhook_events CREATE TABLE, UNIQUE(user_id) ALTER, supabase_realtime ALTER PUBLICATION
</verification>

<success_criteria>
- 4 test stub files exist with all BILL requirement test cases as test.skip()
- Migration 0002 exists with webhook_events table, memberships UNIQUE constraint, and Realtime publication
- vitest reports 0 failures across all 4 test files
</success_criteria>

<output>
After completion, create `.planning/phases/02-billing/02-00-SUMMARY.md`
</output>
